# PPT + 演讲稿 最终版

> 主题：Agent 时代下，Agentic RL 后训练的核心挑战与全栈应对
> 受众：洞察分享会（华为计算部门高级领导）
> 风格：华为/科技企业风格，深色背景
> 总计：15页，每页配60-90秒演讲话术

---

## P1 | 定调层：Agent 时代已来

### PPT内容

**标题**：智能涌现：Agent 从概念验证走向规模落地，AI 产业进入"做事"时代

**核心信息**：产品、开源、资本三个维度同时爆发——Agent 时代不是预测，是正在发生的现实。

```
┌─────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                     │
│  标题：智能涌现：Agent 从概念验证走向规模落地                            │
│  副标题：AI 产业进入"做事"时代                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────┐        │
│  │            顶部：一句话定调（大字，加粗，居中）              │        │
│  │  "从'能聊'到'能做'的分水岭已经跨过，Agent 是载体"           │        │
│  └─────────────────────────────────────────────────────────┘        │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 产品已落地         │ │ 开源正在引爆      │ │ 巨头真金白银      │    │
│  │  ——加速时间线      │ │                  │ │                  │    │
│  │                  │ │ ● Claude Code    │ │ ● Anthropic      │    │
│  │ 2024.3 Devin     │ │   代码泄露事件    │ │   讨论年投$1B+   │    │
│  │  编程Agent,端到端 │ │   (2026.3 npm    │ │   采购RL环境     │    │
│  │  开发             │ │    sourcemap     │ │   (The Info.)    │    │
│  │                  │ │    意外暴露51万行 │ │                  │    │
│  │ 2025.1 Operator  │ │    源码)         │ │ ● OpenAI         │    │
│  │  GUI Agent       │ │                  │ │   RFT 对o4-mini  │    │
│  │  (后升级为       │ │ ● OpenClaw       │ │   公开发布       │    │
│  │   Agent Mode)    │ │   基于泄露代码    │ │   (2025.5)       │    │
│  │                  │ │   社区重写的通用  │ │                  │    │
│  │ 2025.3 Manus     │ │   Agent框架      │ │ ● Google         │    │
│  │  通用Agent,多步骤│ │   一天10万星     │ │   Gemini 2.5整合 │    │
│  │  自主完成        │ │   24.7万GitHub星 │ │   Agent RL训练   │    │
│  │                  │ │   集成20+平台    │ │   多步骤+工具    │    │
│  │ 2025.7           │ │                  │ │                  │    │
│  │  ChatGPT Agent   │ │ ● 开源小模型     │ │ ● Microsoft      │    │
│  │  Mode（替代      │ │   在Agent场景    │ │   Copilot→Agent  │    │
│  │  Operator）      │ │   追平大模型     │ │   编排平台       │    │
│  │                  │ │                  │ │                  │    │
│  │ 2025.10          │ │                  │ │                  │    │
│  │  Claude Computer │ │                  │ │                  │    │
│  │  Use GA          │ │                  │ │                  │    │
│  │                  │ │                  │ │                  │    │
│  │  ↓ 节奏在加快     │ │                  │ │                  │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│          ↓                    ↓                    ↓                │
│  ┌─────────────────────────────────────────────────────────┐        │
│  │ 底部汇聚条：产品从早期探索走向成熟，开源社区用脚投票，       │        │
│  │ 巨头用真金白银下注 → Agent 时代已来                        │        │
│  └─────────────────────────────────────────────────────────┘        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**内容要素**：

- **左列：产品已落地**（时间线呈现加速节奏）
  - 2024.3 Devin：编程 Agent，端到端完成开发任务
  - 2025.1 Operator → 2025.7 ChatGPT Agent Mode
  - 2025.3 Manus：通用 Agent，多步骤任务自主完成
  - 2025.10 Claude Computer Use GA：桌面级 Agent 正式版
- **中列：开源正在引爆**
  - Claude Code 代码泄露：2026.3 npm sourcemap 意外暴露 51.2 万行源码
  - OpenClaw：社区基于泄露代码重写的通用 Agent 框架，24.7 万 GitHub 星
  - 开源小模型在 Agent 场景追平甚至超越大模型
- **右列：巨头真金白银**
  - Anthropic：讨论年投 $1B+ 采购 RL 环境（The Information 报道）
  - OpenAI：RFT 2025.5 对 o4-mini 公开发布
  - Google：Gemini 2.5 训练流程中 RL 阶段支持多步骤动作+工具调用
  - Microsoft：Copilot 从"副驾驶"升级为 Agent 编排平台

### 演讲话术（60-90秒）

> "各位领导，今天不讲趋势综述，直接说结论：Agent 已经从概念验证进入规模落地，产品、开源、资本三条线在 2026 年 Q1 同时爆发。
>
> 先看产品端。请看左列时间线——从 2024 年 Devin 做概念验证，到 2025 年 ChatGPT Agent Mode 替代 Operator，再到 Claude Computer Use 正式版发布，节奏在明显加快。特别是 Cursor Composer 2，一家 IDE 公司用 RL 训练自研模型打败了 Claude Opus 4.6——这说明 Agent RL 已经是产品竞争力的核心。
>
> 再看开源端。中间这列——Claude Code 源码泄露后，社区基于它重写了 OpenClaw 框架，24.7 万 GitHub 星。DeepSWE 用 RL 训练开源 32B 模型达到 Pass@1 42.2%。开源做 Agent 的门槛已经降到可触达。
>
> 最后看资本端。右列——据 The Information 报道，Anthropic 领导层讨论年投超 10 亿美元采购 RL 训练环境。广义后训练算力占总算力份额持续攀升到约 40%。三家路线不同，但共识一致。
>
> 这三条线同时爆发意味着：Agent 后训练不再是实验室项目，而是真实的算力负载——我们的 NPU 平台必须能承接这类负载。这是今天汇报的出发点。"

---

## P2 | 动因层：后训练演进与 Agentic RL 的不可替代性

### PPT内容

**标题**：不可替代：从 SFT 到 Agentic RL，Agent 能力提升只有一条路

```
┌──────────────────────────────────────────────────┐
│ [深色渐变头部条]                                    │
│ 标题：不可替代：从 SFT 到 Agentic RL，             │
│       Agent 能力提升只有一条路                      │
├──────────────────────────────────────────────────┤
│                                                  │
│ 【上半页：三阶段演进横向箭头 + 排除法】【原有+微调】  │
│                                                  │
│ ┌──────────────┐  →  ┌──────────────┐  →  ┌──────────────┐
│ │ 第一阶段       │     │ 第二阶段       │     │ 第三阶段 ★    │
│ │ SFT / RLHF    │     │ DPO / KTO     │     │ Agentic RL   │
│ │               │     │               │     │              │
│ │ 模型：GPT-4   │     │ 模型：Llama3.3│     │ 模型：        │
│ │ Llama-2-Chat  │     │ Qwen-2.5     │     │ GPT-5.4      │
│ │ ChatGLM-3     │     │ Mistral Lg.2 │     │ Claude Op4.6 │
│ │               │     │               │     │ Gemini 3.1Pro│
│ │ 算力：<5%     │     │ 算力：5-15%   │     │ DeepSeek-V3.2│
│ │ 场景：3-5种   │     │ 场景：5-10种  │     │ Llama4 Mav.  │
│ │ (对话/问答)   │     │ (+偏好/安全)  │     │ GLM-5        │
│ │               │     │               │     │              │
│ │ ┌──────────┐ │     │ ┌──────────┐ │     │ 算力：40%+   │
│ │ │✗ 天花板   │ │     │ │✗ 天花板   │ │     │ 场景：50+种  │
│ │ │状态空间大 │ │     │ │奖励来自  │ │     │              │
│ │ │演示覆盖  │ │     │ │环境不来  │ │     │ ┌──────────┐ │
│ │ │不了；    │ │     │ │自偏好；  │ │     │ │✓ 唯一路径 │ │
│ │ │训练分布  │ │     │ │能教对齐  │ │     │ │环境反馈   │ │
│ │ │≠部署分布 │ │     │ │不能教交互│ │     │ │覆盖无限态 │ │
│ │ │(dist.    │ │     │ └──────────┘ │     │ │从错误中学 │ │
│ │ │ shift)   │ │     │              │     │ └──────────┘ │
│ │ └──────────┘ │     └──────────────┘     │              │
│ └──────────────┘                          └──────────────┘
│                                                  │
│ 中间一句话（高亮）：                                │
│ 后训练算力<5%→40%+，Agent必须在做事中学会做事       │
│                                                  │
│ ┌────────────────────────────────────────────────┐│
│ │【桥接层——为什么RL是"唯一闭环方法"】               ││
│ │                                                ││
│ │ 金句（高亮）：                                   ││
│ │ "Harness的能力图，就是未来模型后训练的负载"       ││
│ │                                                ││
│ │ 公式：Agent能力 = 模型 + Harness               ││
│ │ 方向：Harness今天做的 → 模型明天要内化的         ││
│ │ 唯一性：SFT/DPO教静态映射；                      ││
│ │         RL = 唯一能在环境闭环中吸收能力的方法     ││
│ │                                                ││
│ │ 迁移实证（仅保留一行）：                          ││
│ │ ┌──────────────────────────────────────────┐   ││
│ │ │ 上下文压缩：Harness侧规则 → 模型内生能力  │   ││
│ │ │ Cursor Composer 2 用RL训self-summarization│   ││
│ │ │ 压缩错误↓50%，5000+token → ~1000token     │   ││
│ │ └──────────────────────────────────────────┘   ││
│ └────────────────────────────────────────────────┘│
│                                                  │
│ 【下半页：飞轮 + 巨头验证】                         │
│                                                  │
│    ┌──────────┐                                  │
│    │ 模型更强  │←────────────────┐                │
│    └────┬─────┘                 │                │
│         ↓                       │                │
│  ┌────────────┐          ┌──────┴─────┐          │
│  │在环境中做事 │          │更新模型参数  │          │
│  │(Harness)   │          │            │          │
│  └──────┬─────┘          └──────┬─────┘          │
│         │                       ↑                │
│   Anthropic                Google                │
│   $1B+采购环境             Gemini全流程RL         │
│         │                       ↑                │
│         ↓                       │                │
│  ┌────────────┐          ┌──────┴─────┐          │
│  │产生交互轨迹 │─────────→│得到环境反馈  │          │
│  └────────────┘          └────────────┘          │
│                                ↑                  │
│                           OpenAI                  │
│                           RFT开放grader           │
│                                                  │
│  三家全部押注RL后训练，没有一家认为SFT够用          │
│                                                  │
│ ┌──────────────────────────────────────────────┐ │
│ │ 结论（高亮）：                                  │ │
│ │ Agentic RL后训练 = Agent能力提升的不可替代路径  │ │
│ │ → 但直接把LLM RL搬到Agent场景，会结构性失配    │ │
│ │ → 接下来看：七维差异沿R3数据流催生哪五大挑战？  │ │
│ └──────────────────────────────────────────────┘ │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "P1 我们看到 Agent 时代已经来了。那凭什么说 Agentic RL 后训练是关键？这一页用排除法回答。
>
> 请看上半页三个阶段。第一代 SFT/RLHF，天花板是训练分布不等于部署分布——Agent 的状态空间太大，演示覆盖不了。第二代 DPO/KTO，天花板是奖励来自环境验证而非人类偏好——能教对齐不能教交互。排除完前两条路，只剩 Agentic RL：环境反馈覆盖无限状态，从错误中学习。
>
> 排除法告诉我们只剩 RL 一条路，但**为什么 RL 就能行**？
>
> 一个关键洞察：Agent 的能力等于模型加上 Harness——就是围绕模型的工程脚手架。今天 Harness 做的上下文压缩、工具编排、错误恢复，明天都要被模型内化。换句话说，**Harness 的能力图，就是未来模型后训练的负载**。
>
> SFT 只能教静态输入输出映射，DPO 只能教偏好排序，它们都无法让模型在做事中学会做事。RL 是唯一能通过环境闭环让模型吸收这些动态能力的方法。
>
> 一个已经落地的实证：Cursor Composer 2 用 RL 训练 self-summarization，把上下文压缩从 Harness 侧的工程方案变成模型的内生能力，压缩错误降了 50%，摘要长度从 5000 多 token 压到 1000 左右。
>
> 再看下半页飞轮——Anthropic 投环境、OpenAI 开放 grader、Google 做全流程 RL。三家全部押注 RL 后训练，没有一家认为 SFT 够用。
>
> 所以结论很清楚：Agentic RL 后训练是 Agent 能力提升的不可替代路径。但——直接把传统 LLM RL 搬到 Agent 场景，会结构性失配。接下来我们看，到底哪里不同。"

---

## P3 | 七维变化 → 五挑战+横切（直接展示）

### PPT内容

**标题**：范式跃迁：七维结构性变化催生五大挑战+横切约束

**核心信息**：左半页用七维对比表展示LLM RL与Agentic RL的差异，右半页直接按五个挑战展示（不按层分组），每个挑战一行：名称+来源维度+量化严重性。底部过渡到P4三层架构升级。挑战本身不预设分层——分层是P4解法的组织方式。

**P2→P3 承接**：P2 论证了"必须走 Agentic RL"，P3 回答"Agentic RL 和 LLM RL 到底有什么不同？这些差异直接造成哪五大挑战？"

```
┌──────────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                          │
│  标题：范式跃迁：七维变化 → 五大挑战+横切                                  │
├────────────────────────────┬─────────────────────────────────────────────┤
│                            │                                             │
│  【左半页 40%】             │  【右半页 60%】                               │
│  LLM RL vs Agentic RL     │  五大挑战——直接从维度正推                     │
│  七维对比表                 │  每行：名称 | 来源维度 | 量化严重性            │
│                            │                                             │
│  ┌──────────────────────┐  │  ┌──────────────────────────────────────┐   │
│  │ 维度   │LLM RL│Agntc RL│  │  │                                      │   │
│  │ ───────┼──────┼────────│  │  │  ❶ 长序列推理开销                      │   │
│  │ ①算力  │训练主│推理90% │  │  │     维度①+⑦ │ 单Agent会话KV数GB      │   │
│  │ ②环境  │无    │真实    │  │  │     千并发撑爆显存，70B不量化放不下     │   │
│  │ ③数据  │标注  │在线轨迹│  │  │     ★★★★★（最高频痛点）               │   │
│  │ ④吞吐  │离线  │在线流式│  │  │                                      │   │
│  │ ⑤奖励  │人评  │环境验证│  │  │  ❷ 环境交互延迟                        │   │
│  │ ⑥探索  │KL约束│大探索  │  │  │     维度②+③ │ Episode分钟级          │   │
│  │ ⑦上下文│短    │M级长程 │  │  │     工具调用长尾拖拽rollout            │   │
│  │                        │  │  │     ★★★★☆                             │   │
│  │  七维差异 ────────→     │  │  │                                      │   │
│  │  直接正推出五个挑战     │  │  │  ❸ 在线数据吞吐瓶颈                    │   │
│  └──────────────────────┘  │  │     维度④ │ 异步数据天然过时            │   │
│                            │  │     版本差>4步发散（SkyRL）              │   │
│                            │  │     ★★★★☆                               │   │
│                            │  │                                      │   │
│                            │  │  ❹ 探索效率                              │   │
│                            │  │     维度⑥ │ 84%算力浪费在非关键步骤     │   │
│                            │  │     （CSO反推：仅16%步骤关键）           │   │
│                            │  │     ★★★☆☆                               │   │
│                            │  │                                      │   │
│                            │  │  ❺ 稀疏延迟奖励的归因                    │   │
│                            │  │     维度⑤ │ 轨迹级奖励太粗             │   │
│                            │  │     无法区分"碰巧做对"和"真正学会"       │   │
│                            │  │     ★★★☆☆                               │   │
│                            │  └──────────────────────────────────────┘   │
│                            │                                             │
│                            │  ┌─ ⚪横切：数值可信 ─────────────────┐     │
│                            │  │  穿透全部五个挑战                    │     │
│                            │  │  RL对数值噪声极敏感，底层不可信      │     │
│                            │  │  上层调参就变玄学                    │     │
│                            │  └──────────────────────────────────┘     │
│                            │                                             │
├────────────────────────────┴─────────────────────────────────────────────┤
│  【底部过渡条（高亮）】                                                     │
│  这5个挑战驱动后训练软件栈三层架构八大升级 → P4展开                        │
└──────────────────────────────────────────────────────────────────────────┘
```

**视觉设计要点**：
- 右半页不按层分组，五个挑战平铺展示，每个挑战一行
- 每行三要素：挑战名称、来源维度编号、量化严重性数据+星级标注
- 横切约束用白色/灰色边框横跨底部，表示穿透全部五个挑战
- 左半页对比表七维不分组——维度是原因，挑战是结果，因果箭头直接指向右侧
- 挑战不预设分层，分层是P4解法的组织方式
- 底部过渡句明确"5挑战驱动三层架构升级"，为P4搭桥

### 演讲话术（60-90秒）

> "上一页我们说，直接把LLM RL搬到Agent场景会结构性失配。失配在哪？我把七个维度摊开，直接正推出五个挑战加一个横切约束。
>
> 左边七维对比，右边五个挑战，逐个看。
>
> 第一，长序列推理开销。维度一算力加维度七上下文联合驱动——Agent会话百万级token，单会话KV Cache数个GB，千并发直接撑爆显存。70B模型不量化单卡放不下。这是最高频的痛点。
>
> 第二，环境交互延迟。维度二环境加维度三数据驱动——Agent的Episode动辄分钟级，工具调用的长尾延迟拖拽整条rollout。
>
> 第三，在线数据吞吐瓶颈。维度四驱动——训推分离后数据天然异步产生，版本差超过4步就发散，SkyRL实测数据。
>
> 第四，探索效率。维度六驱动——84%的rollout算力浪费在非关键步骤上，CSO的研究表明仅16%步骤是关键的。
>
> 第五，稀疏延迟奖励的归因。维度五驱动——轨迹级奖励太粗，分钟级轨迹里到底哪一步起了作用？无法区分'碰巧做对'和'真正学会'。
>
> 最后，横切约束——数值可信。穿透全部五个挑战。RL对数值噪声极敏感，底层精度不可信，上层一切调参都是玄学。
>
> 底部这句话最关键：**这五个挑战驱动后训练软件栈三层架构八大升级**。下一页展开。"

---

## P4 | 五挑战→八升级方向的因果传导 + 三层架构

### PPT内容

**标题**：五挑战驱动八个架构升级方向——后训练软件栈三层全面升级

**核心信息**：三列因果流（5挑战→8升级方向→对应技术）配合底部三层架构锚条，一页打通"为什么升级、升级什么、谁在做"。

**P3→P4 承接**：P3 推导出五挑战+横切，P4 回答"这些挑战驱动哪些架构升级？每个升级为什么是必然的？业界给出了什么方案？"

```
┌──────────────────────────────────────────────────────────────────────┐
│  五挑战 → 八升级方向 — 后训练软件栈三层全面升级                        │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─ 5挑战 ────┐   ┌─ 8升级方向(层标色) ──────────┐  ┌─ 技术 ──────┐│
│  │             │   │                              │  │             ││
│  │ ❶长序列    │→  │ 🔵a KV Cache池化 [基础设施]   │→ │ LMCache     ││
│  │  推理开销  │┐  │   因为:KV数GB 否则:显存爆     │  │ Mooncake    ││
│  │            ││  ├──────────────────────────────┤  ├─────────────┤│
│  ├────────────┤├→ │ 🔵b 推理加速 [基础设施]      │→ │ Eagle3      ││
│  │ ❷环境交互 │┤  │   因为:70B放不下 否则:成本崩  │  │ 4bit量化    ││
│  │  延迟     │┤  ├──────────────────────────────┤  ├─────────────┤│
│  │            │├→ │ 🔵c sandbox并行 [基础设施]    │→ │ Forge       ││
│  ├────────────┤│  │   因为:EP分钟级 否则:吞吐塞  │  │             ││
│  │ ❸在线数据 │┤  ├──────────────────────────────┤  ├─────────────┤│
│  │  吞吐     │├→ │ 🟢d 训推解耦 [框架]          │→ │ ProRL Agent ││
│  │            ││  │   因为:I/O阻塞 否则:GPU空转   │  │             ││
│  ├────────────┤│  ├──────────────────────────────┤  ├─────────────┤│
│  │ ❹探索效率 │├→ │ 🟢e 异步调度 [框架]          │→ │ Dynamo      ││
│  │            ││  │   因为:流式产生 否则:等待空转  │  │             ││
│  ├────────────┤│  ├──────────────────────────────┤  ├─────────────┤│
│  │ ❺稀疏奖励 │├→ │ 🟠f off-policy容忍 [算法]    │→ │ Dec.PPO     ││
│  │  归因     │┘  │   因为:数据过时 否则:训练发散  │  │ DAPO/CISPO  ││
│  │            │   ├──────────────────────────────┤  ├─────────────┤│
│  └────────────┘   │ 🟠g 步级归因 [算法]          │→ │ Cursor+Meta ││
│                    │   因为:奖励太粗 否则:84%浪费  │  │             ││
│  ❶也驱动 ─────→   ├──────────────────────────────┤  ├─────────────┤│
│  (跨层箭头)        │ 🟠h 上下文自压缩 [算法]      │→ │ Cursor self-││
│                    │   因为:M级上下文推理爆炸      │  │ summariz.   ││
│                    │   否则:外部truncation丢信息   │  │ ACON        ││
│                    └──────────────────────────────┘  └─────────────┘│
│                                                                      │
│  ┌─ ⚪横切：数值精度保证 ── 穿透全部三层 ────────────────────────┐  │
│  │ 训推一致性 / 确定性计算 / 精度基线 / 算子自动生成               │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─ 三层架构锚 ─────────────────────────────────────────────────┐  │
│  │ 🔵基础设施层 a+b+c   │ 🟢框架层 d+e     │ 🟠算法层 f+g+h   │  │
│  │ ❶❷驱动               │ ❷❸驱动           │ ❶❹❺驱动          │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ▶ 五挑战驱动八升级,每层都有因果必然 → 先看基础设施层                  │
└──────────────────────────────────────────────────────────────────────┘
```

**视觉设计要点**：
- 三列因果流：左列5挑战、中列8升级方向（按层标色：蓝/绿/橙）、右列对应技术
- 箭头从挑战射入升级方向，展示因果驱动关系（一个挑战可驱动多个方向）
- 每个升级方向内嵌"因为/否则"一句话因果
- 底部架构锚条：三层一行排列，标注驱动挑战编号，一目了然
- 横切约束横跨三层，穿透全部
- 色标与P3对应：基础设施层蓝色、框架层绿色、算法层橙色

### 演讲话术（60-90秒）

> "请看这页。左列是五个挑战，中列是八个升级方向——注意颜色，蓝色是基础设施层、绿色框架层、橙色算法层。箭头展示因果：每个挑战射入它驱动的升级方向。比如挑战1长序列推理开销，射入蓝色区的KV池化和推理加速，同时也射入橙色区的上下文自压缩——因为M级上下文不仅撑爆显存，也让推理爆炸，模型必须学会自主压缩。右列是每个升级方向对应的业界技术方案。底部架构锚条一目了然：基础设施层3个升级、框架层2个、算法层3个。横切约束穿透全部三层。接下来从底层开始，逐层展开。"

---

## P5 | 基础设施层升级概览（挑战❶+❷驱动）

### PPT内容

**标题**：基础设施层升级：KV池化+推理加速+sandbox并行（挑战❶+❷驱动）

```
┌─────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                     │
│  标题：🔵 基础设施层升级：KV池化+推理加速+sandbox并行                   │
│  副标题：挑战❶长序列推理开销 + 挑战❷环境交互延迟 驱动                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区（左侧 60%）             张力示意（右侧 40%）      │     │
│  │                                                            │     │
│  │  基础设施层承载两个挑战：        ┌────────┐  ←对抗→ ┌──────┐│     │
│  │                                   │ 显存    │         │并发  ││     │
│  │  ❶长序列推理开销：               │ 容量    │         │ 规模 ││     │
│  │  M级上下文KV Cache数GB，          └────────┘         └──────┘│     │
│  │  千并发撑爆显存；70B模型                                     │     │
│  │  不量化单卡放不下。                 ┌────────┐  ←对抗→ ┌──────┐│     │
│  │                                   │ rollout │         │吞吐  ││     │
│  │  ❷环境交互延迟：                  │ 时长    │         │      ││     │
│  │  Episode分钟级，sandbox            └────────┘         └──────┘│     │
│  │  串行执行拖拽整体吞吐。                                      │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 🔵a KV Cache池化  │ │ 🔵b 推理加速      │ │ 🔵c sandbox并行   │    │
│  │                  │ │                  │ │                  │    │
│  │ LMCache          │ │ Eagle3           │ │ Forge            │    │
│  │ +Mooncake        │ │ +4bit量化        │ │ MiniMax          │    │
│  │                  │ │                  │ │                  │    │
│  │ 跨引擎P2P共享   │ │ 投机推理+量化    │ │ Agent脚手架与    │    │
│  │ KV cache池化     │ │ 70B→单卡可放    │ │ 训练引擎解耦     │    │
│  │ RDMA跨节点传输   │ │ 推理成本可控     │ │ 树状合并40x加速  │    │
│  │                  │ │                  │ │                  │    │
│  │ → P6 详细展开    │ │ → P7 详细展开    │ │ → P8 详细展开    │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│                                                                     │
│  ▶ 从最底层开始：先看KV Cache怎么池化                                │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "P4我们看到五挑战驱动八升级、三层架构。现在从最底层——基础设施层开始逐层展开。
>
> 基础设施层由挑战❶和❷驱动，承载三个升级方向。
>
> 先看挑战❶长序列推理开销。请看右上角对抗关系——显存容量和并发规模不可兼得。M级上下文的KV Cache数GB级，千并发撑爆显存；70B模型不量化单卡放不下。
>
> 再看挑战❷环境交互延迟。Episode分钟级，sandbox串行执行拖拽整体吞吐——rollout时长和吞吐不可兼得。
>
> 怎么解？下面三个卡片。第一，KV Cache池化——LMCache做跨引擎P2P共享，Mooncake做RDMA跨节点KV池化，让KV不再是引擎私有资源。第二，推理加速——Eagle3投机推理加4bit量化，让70B模型单卡可放，推理成本可控。第三，sandbox并行化——MiniMax Forge做Agent脚手架与训练引擎解耦，树状合并策略实现40倍加速。
>
> 三个方向从存储、计算、环境三个维度同时解决基础设施层的瓶颈。接下来三页逐一展开。"

---

## P6 | LMCache + Mooncake（KV Cache池化）

### PPT内容

**标题**：LMCache + Mooncake：从节点孤岛到集群级KV Cache池
**标签**：🔵基础设施层 · 升级方向a · KV Cache池化 · 挑战❶驱动

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：LMCache + Mooncake：从节点孤岛到集群级KV Cache池               │
│  标签：🔵基础设施层 · 升级方向a · 挑战❶长序列推理开销                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】两套互补    │ │
│  │                                      │  │  方案                │ │
│  │  Agent长程对话场景下，KV Cache:        │  │                     │ │
│  │  - 单会话可达数GB                     │  │  ● LMCache          │ │
│  │  - 千并发撑爆GPU显存                  │  │    跨引擎P2P共享     │ │
│  │  - 每个推理引擎独立维护KV cache       │  │    KV cache          │ │
│  │  - 同一prefix被多引擎重复计算          │  │    集成vLLM/SGLang   │ │
│  │  - Agent场景95%+context可复用         │  │    /KServe           │ │
│  │                                      │  │    多轮QA 15x吞吐    │ │
│  │  传统方案把KV Cache绑定在             │  │    TTFT降低54.7%     │ │
│  │  生成它的GPU上——                     │  │                     │ │
│  │  扩展性受单机显存硬约束               │  │  ● Mooncake         │ │
│  │                                      │  │    月之暗面Kimi      │ │
│  │  ┌────────────────────────┐          │  │    生产系统          │ │
│  │  │ 瓶颈示意：              │          │  │    KVCache-centric   │ │
│  │  │ GPU_A: [KV][KV][KV]满 │          │  │    Disaggregated     │ │
│  │  │ GPU_B: [  ][  ][  ]空 │          │  │    Architecture      │ │
│  │  │        ↑ 无法迁移       │          │  │    RDMA+分布式       │ │
│  │  └────────────────────────┘          │  │    KV Store          │ │
│  │                                      │  │    Prefill-Decode    │ │
│  │                                      │  │    分离              │ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：KV cache不应是引擎私有资源，而应是集群级共享池。     │    │
│  │  LMCache做引擎间P2P共享，Mooncake做架构级KV分离池化。          │    │
│  │  月之暗面已在Kimi生产环境验证——不是学术概念。                   │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU推理架构需支持KV Cache分离和跨节点池化，              │    │
│  │  标准化KV cache存取接口+高效跨节点传输，HCCS是天然优势         │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "基础设施层第一个升级方向——KV Cache池化，由挑战❶长序列推理开销驱动。两个方案互补。
>
> 先看问题——Agent长程对话下单会话KV Cache可达数GB，千并发撑爆显存。更关键的是，每个推理引擎独立维护KV cache，同一prefix被多引擎重复计算，Agent场景下95%以上context可复用，浪费巨大。
>
> LMCache解决引擎间共享——开源项目，集成vLLM、SGLang、KServe，P2P直接传输KV cache加多级卸载。实测多轮QA场景15倍吞吐提升，首token延迟降低54.7%。
>
> Mooncake解决架构级分离——月之暗面Kimi生产系统架构，以KV Cache为中心做disaggregated architecture，RDMA跨节点传输存入分布式KV Store，Prefill-Decode分离各自优化。这不是学术论文概念，是真实线上流量验证过的方案。
>
> 关键逻辑：KV cache从引擎私有资源变为集群级共享池。对我们的启示——NPU推理架构需支持KV Cache分离和跨节点池化，HCCS互联是天然优势。"

---

## P7 | Eagle3 + 4bit量化（推理加速）

### PPT内容

**标题**：Eagle3 + 4bit量化：投机推理×量化，让70B模型推理成本可控
**标签**：🔵基础设施层 · 升级方向b · 推理加速 · 挑战❶+❷驱动

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Eagle3 + 4bit量化：投机推理×量化，70B推理成本可控                │
│  标签：🔵基础设施层 · 升级方向b · 挑战❶+❷驱动                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】方案        │ │
│  │                                      │  │                     │ │
│  │  Agent RL推理侧的双重瓶颈：           │  │  ● Eagle3           │ │
│  │                                      │  │    (投机推理)        │ │
│  │  ① 70B模型不量化单卡放不下            │  │    无需微调draft     │ │
│  │    → 多卡推理通信开销大               │  │    模型，直接从      │ │
│  │    → 单次推理成本高                   │  │    目标模型特征层    │ │
│  │                                      │  │    构建轻量预测头    │ │
│  │  ② Agent场景推理调用量巨大            │  │    接受率提升        │ │
│  │    每个Episode数十到数百次            │  │    2.5-3x加速       │ │
│  │    工具调用+思考步骤叠加               │  │                     │ │
│  │    推理成本占训练总成本60%+           │  │  ● 4bit量化          │ │
│  │                                      │  │    (GPTQ/AWQ)        │ │
│  │  ③ 推理延迟直接影响rollout速度        │  │    70B → 单卡可放   │ │
│  │    工具调用长尾延迟拖拽整条rollout     │  │    显存占用降75%     │ │
│  │                                      │  │    精度损失<1%       │ │
│  │  ┌────────────────────────┐          │  │    (任务相关基准)    │ │
│  │  │ 成本示意：              │          │  │                     │ │
│  │  │ 70B FP16: 140GB显存   │          │  │  ● 两者可组合       │ │
│  │  │ → 至少2×A100/H100     │          │  │    量化后投机推理    │ │
│  │  │ 70B 4bit: ~35GB显存   │          │  │    成本↓+速度↑       │ │
│  │  │ → 单卡可放             │          │  │    Agent RL训练     │ │
│  │  └────────────────────────┘          │  │    推理成本可控      │ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：Agent RL的推理成本占总成本60%+，推理加速是           │    │
│  │  降低训练总成本的最大杠杆。量化解决"放得下"，投机推理解决        │    │
│  │  "跑得快"，两者组合让70B模型在Agent场景经济可行。                │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU需原生支持4bit量化推理和投机推理（speculative          │    │
│  │  decoding）的高效实现，这是Agent RL场景的成本关键路径            │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "基础设施层第二个升级方向——推理加速，同样由挑战❶❷驱动。问题很直接：70B模型FP16需要140GB显存，至少两张A100/H100。Agent场景每个Episode数十到数百次推理调用，推理成本占训练总成本60%以上。
>
> 两个互补方案。Eagle3做投机推理——无需微调draft模型，直接从目标模型特征层构建轻量预测头，接受率提升带来2.5到3倍加速。4bit量化（GPTQ/AWQ）让70B模型从140GB降到约35GB，单卡可放，精度损失在任务相关基准上不到1%。
>
> 关键是两者可以组合——先量化放得下，再投机推理跑得快。这是降低Agent RL训练总成本的最大杠杆。
>
> 对我们的启示——NPU需要原生支持4bit量化推理和投机推理的高效实现，这是Agent RL场景的成本关键路径。"

---

## P8 | Forge（sandbox并行化）

### PPT内容

**标题**：MiniMax Forge：Agent脚手架与训练引擎解耦，sandbox并行40x加速
**标签**：🔵基础设施层 · 升级方向c · sandbox并行化 · 挑战❷驱动

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Forge：sandbox并行化 + 行业空白                                 │
│  标签：🔵基础设施层 · 升级方向c · 挑战❷环境交互延迟                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区（左侧 60%）             空白示意（右侧 40%）     │     │
│  │                                                            │     │
│  │  sandbox并行化承载挑战❷：        ┌────────┐ ←→ ┌──────┐  │     │
│  │                                   │探索效率 │     │安全性│  │     │
│  │  Episode分钟级，串行执行           └────────┘     └──────┘  │     │
│  │  让rollout吞吐成为瓶颈。                                    │     │
│  │  Agent在探索中可能陷入死循环、     算法层：2个方案 ●●        │     │
│  │  冗余操作、永不终止——             框架层：2个方案 ●●        │     │
│  │  消耗算力却无有效信号。             基础设施层：              │     │
│  │                                    1个方案 ● + ⚠️ 空白     │     │
│  │  此外：算法2-4周迭代，                                      │     │
│  │  但算子适配3-6个月。                                        │     │
│  │  开放任务的奖励定义模糊，                                    │     │
│  │  并行探索越多越容易发现                                      │     │
│  │  reward hack捷径。                                          │     │
│  │                                                            │     │
│  │  回看P4的3-2-3分布：基础设施层最薄弱                         │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌──────────────────────┐ │
│  │  唯一方案：MiniMax Forge分层设计       │  │  ⚠️ 行业空白         │ │
│  │                                      │  │                      │ │
│  │  · Agent脚手架与训练引擎完全解耦       │  │  sandbox保真度验证   │ │
│  │  · 支持任意Agent脚手架+工具            │  │  可控探索平台         │ │
│  │  · 树状样本合并策略→40x加速            │  │  大规模环境编排       │ │
│  │  · M2.5: 20万+真实环境训练             │  │  reward hack检测     │ │
│  │    日吞吐百万级, SWE-Bench 80.2%      │  │                      │ │
│  │                                      │  │  尚无生产级方案       │ │
│  │  Forge解决了sandbox并行化             │  │  → 见P15：这正是     │ │
│  │  和工程可达，但sandbox保真             │  │    我们的切入机会     │ │
│  │  仍是空白                             │  │                      │ │
│  └──────────────────────────────────────┘  └──────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑（高亮条）：                                         │    │
│  │  Forge解决sandbox并行化和工程可达，但sandbox保真仍是空白。      │    │
│  │  算法层和框架层卷得厉害，基础设施层几乎没人做——                  │    │
│  │  这恰恰是硬件平台方的天然主场。                                │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU作为硬件平台方，在基础设施层（sandbox保真、           │    │
│  │  可控探索、精度一致性）有天然优势——这不是追赶，是卡位。         │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "基础设施层第三个升级方向——sandbox并行化，由挑战❷环境交互延迟驱动。这一页的叙事跟前两页不同——不只是展开技术，而是同时**指出行业空白**。
>
> Episode分钟级，串行执行让rollout吞吐成瓶颈。Agent探索中可能死循环、冗余操作、永不终止——消耗算力却无有效信号。回看P4架构锚条：基础设施层最薄弱。
>
> 唯一的方案是MiniMax的Forge分层设计——Agent脚手架与训练引擎完全解耦，树状样本合并策略实现40倍加速，M2.5在20万以上真实环境训练，SWE-Bench达80.2%。但Forge解决了sandbox并行化和工程可达，sandbox保真——保真度验证、可控探索平台、大规模环境编排——仍是**行业空白**。
>
> 这对我们意味着什么？算法层和框架层已经被ByteDance、NVIDIA、MiniMax等巨头占满，我们从零追赶。但基础设施层——sandbox保真、可控探索、精度一致性——几乎没人做。这不是追赶，是卡位。
>
> 基础设施层三个方向展开完毕。接下来往上走，进入框架层。"

---

## P9 | 框架层升级概览（挑战❷+❸驱动）

### PPT内容

**标题**：框架层升级：训推解耦+异步调度（挑战❷+❸驱动）

```
┌─────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                     │
│  标题：🟢 框架层升级：训推解耦+异步调度                                 │
│  副标题：挑战❷环境交互延迟 + 挑战❸在线数据吞吐 驱动                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区                          张力示意               │     │
│  │                                                            │     │
│  │  训推分离把环境交互全放到           ┌────────┐ ←→ ┌──────┐ │     │
│  │  推理侧。双重复杂度叠加：          │有状态   │     │高吞吐│ │     │
│  │                                   └────────┘     └──────┘ │     │
│  │  ❷环境交互延迟：                                           │     │
│  │  Rollout与训练耦合，环境I/O阻塞GPU                          │     │
│  │  多轮Agent交互延迟层层累积                                   │     │
│  │                                                            │     │
│  │  ❸在线数据吞吐：                                           │     │
│  │  数据在线流式产生，异步调度不到位                              │     │
│  │  则训练等推理、推理等环境，全链路空转                          │     │
│  │                                                            │     │
│  │  单独看都能解，叠在一起不是线性增长的复杂度                    │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌──────────────────────────────┐ ┌──────────────────────────────┐  │
│  │ 🟢d 训推解耦                  │ │ 🟢e 异步调度                  │  │
│  │ (Rollout服务化)               │ │ (KV感知路由+动态调度)         │  │
│  │                              │ │                              │  │
│  │ ProRL Agent (NVIDIA)         │ │ NVIDIA Dynamo                │  │
│  │ Rollout-as-a-Service         │ │ KV-aware Router              │  │
│  │ 三阶段异步流水线              │ │ Radix Tree追踪KV分布         │  │
│  │ INIT/RUN/EVAL                │ │ Prefill-Decode分离           │  │
│  │ → 应对❷环境I/O阻塞GPU       │ │ → 应对❸数据流式产生空转       │  │
│  │                              │ │                              │  │
│  │ → P10 详细展开               │ │ → P11 详细展开               │  │
│  └──────────────────────────────┘ └──────────────────────────────┘  │
│                                                                     │
│  ▶ 框架层两个方向——先看训推解耦怎么做                                 │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "基础设施层三个方向展开完毕，往上走进入框架层。框架层由挑战❷和❸驱动，承载两个升级方向。
>
> 挑战❷在基础设施层已经解决了显存和推理成本问题，但框架层面还有架构耦合——Rollout与训练耦合，环境I/O阻塞GPU，多轮Agent交互延迟层层累积。这不是算法能修的，必须从框架层做架构解耦。
>
> 挑战❸在线数据吞吐——数据在线流式产生，如果调度不到位，训练等推理、推理等环境，全链路空转。
>
> 两个方向。第一，训推解耦——NVIDIA ProRL Agent做Rollout-as-a-Service，三阶段异步流水线INIT/RUN/EVAL完全解耦，应对❷环境I/O阻塞。第二，异步调度——NVIDIA Dynamo做KV感知路由，用Radix Tree追踪全集群KV分布，配合Prefill-Decode分离，应对❸数据流式产生的空转问题。
>
> 接下来两页逐一展开。"

---

## P10 | ProRL Agent（训推解耦）

### PPT内容

**标题**：ProRL Agent：Rollout-as-a-Service，NVIDIA终结耦合式Agent RL
**标签**：🟢框架层 · 升级方向d · 训推解耦 · NVIDIA（2026.03发布）

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：ProRL Agent：Rollout-as-a-Service，终结耦合式Agent RL         │
│  标签：🟢框架层 · 升级方向d · 训推解耦 · NVIDIA（2026.03）             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】方案        │ │
│  │                                      │  │                     │ │
│  │  传统Agent RL（如SkyRL/VeRL-Tool）   │  │  ● Rollout-as-a-   │ │
│  │  把Rollout耦合在Trainer内：           │  │    Service架构      │ │
│  │  - 环境I/O阻塞GPU训练               │  │    三阶段异步流水线  │ │
│  │  - GPU空等环境返回结果               │  │    INIT/RUN/EVAL    │ │
│  │  - 多轮Agent交互延迟累积             │  │                     │ │
│  │                                      │  │  ● 集成NeMo Gym    │ │
│  │  这是框架级的架构问题——               │  │    支持Singularity  │ │
│  │  不是算法能修的，必须从框架层解耦     │  │    沙箱环境          │ │
│  │                                      │  │                     │ │
│  │  ┌────────────────────────┐          │  │  ● Token ID全链路   │ │
│  │  │ 耦合式瓶颈示意：        │          │  │    透传，策略版本    │ │
│  │  │ GPU: [训练][等][训练][等]│          │  │    精确追踪          │ │
│  │  │      ↑环境I/O阻塞GPU    │          │  │                     │ │
│  │  └────────────────────────┘          │  │  ● Qwen3-8B:       │ │
│  │                                      │  │    SWE-Bench        │ │
│  │                                      │  │    9.6%→18.0%(近2x)│ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：Rollout必须从Trainer中解耦为独立服务，才能释放GPU   │    │
│  │  这是框架级解耦，不是算法修正                                  │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU训练框架需支持Rollout-as-a-Service解耦架构，        │    │
│  │  环境交互不阻塞训练计算，Token ID全链路透传                    │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "框架层第一个升级方向——训推解耦。ProRL Agent，NVIDIA 2026年3月发布。
>
> 请看左侧问题——NVIDIA论文直接点名了SkyRL和VeRL-Tool等传统方案的耦合缺陷：Rollout耦合在Trainer内，GPU空等环境返回结果，多轮Agent交互延迟层层累积。这不是调算法能修的，必须从框架层做架构解耦。
>
> 右侧是NVIDIA的方案：Rollout-as-a-Service——把Rollout提升为独立服务，三阶段异步流水线INIT/RUN/EVAL完全解耦。集成NeMo Gym，支持Singularity沙箱环境。关键设计是Token ID全链路透传，策略版本精确追踪。
>
> 实测结果：Qwen3-8B在SWE-Bench从9.6%提升到18.0%，接近2倍。
>
> 关键逻辑：这是框架级解耦——释放GPU不再空等环境I/O，同时因为数据更新鲜也缓解了off-policy问题。对我们的启示——NPU训练框架需支持Rollout-as-a-Service解耦架构。"

---

## P11 | Dynamo（异步调度）

### PPT内容

**标题**：NVIDIA Dynamo：KV 感知路由+异步调度，让推理集群不空转
**标签**：🟢框架层 · 升级方向e · 异步调度 · NVIDIA GTC 2025

**问题**：Agent 多轮对话中大量 prefix 可复用，但标准负载均衡不知道哪个节点有哪段 KV cache，导致重复计算。数据在线流式产生，调度不到位则训练等推理空转。

**方案**：
- NVIDIA GTC 2025 发布，开源推理框架
- KV-aware Router：Radix Tree 追踪全集群 KV 分布，计算 cache overlap score
- NIXL：点对点 KV cache 传输库
- KV Block Manager：多层级内存管理（GPU VRAM → CPU RAM → SSD）
- Prefill-Decode 分离
- 动态调度：根据负载和KV分布实时调整请求路由

**关键逻辑**：推理调度从"哪个节点空闲"变成"哪个节点已有我需要的 KV"，同时实现异步流水线不空转

**启示**：NPU 推理引擎需要支持 KV cache 元数据暴露、跨节点迁移能力和异步调度

### 演讲话术（60-90秒）

> "框架层第二个升级方向——异步调度。NVIDIA Dynamo，GTC 2025发布的开源推理框架。
>
> 核心问题：Agent多轮对话大量prefix可复用，但标准负载均衡只看哪个节点空闲，不知道哪个节点有我需要的KV cache。结果是同一段上下文被不同节点重复计算。更关键的是，数据在线流式产生，调度不到位则训练等推理、推理等环境，全链路空转。
>
> Dynamo的方案是KV-aware Router——用Radix Tree追踪全集群KV分布，计算cache overlap score，把请求路由到已有缓存的节点。配合NIXL点对点传输库和多层级内存管理，实现Prefill-Decode分离和动态异步调度。
>
> 关键逻辑：推理调度从'哪个节点空闲'变成'哪个节点已有我需要的KV'，让整个推理集群不空转。对我们的启示——NPU推理引擎需要支持KV cache元数据暴露、跨节点迁移能力和异步调度，这是Agent推理场景的刚需。
>
> 框架层两个方向展开完毕。接下来进入最上层——算法层。"

---

## P12 | 算法层升级概览（挑战❹+❺驱动）

### PPT内容

**标题**：算法层升级：off-policy容忍+步级归因+上下文自压缩（挑战❶+❹+❺驱动）

```
┌─────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                     │
│  标题：🟠 算法层升级：off-policy容忍+步级归因+上下文自压缩               │
│  副标题：挑战❶长序列推理开销 + 挑战❹探索效率 + 挑战❺稀疏延迟奖励归因 驱动│
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区（左侧 60%）             张力示意（右侧 40%）      │     │
│  │                                                            │     │
│  │  算法层承载三个挑战：           ┌────────┐  ←对抗→ ┌──────┐│     │
│  │                                 │ 吞吐量  │         │新鲜度││     │
│  │  ❶长序列推理开销（算法侧）：     │ ↑↑↑↑   │         │ ↑↑↑ ││     │
│  │  M级上下文推理开销爆炸，          └────────┘         └──────┘│     │
│  │  外部truncation丢信息。             ┌────────┐  ←对抗→ ┌──────┐│     │
│  │                                    │ 归因    │         │效率  ││     │
│  │  ❹探索效率：                       │ 精度    │         │      ││     │
│  │  异步数据天然过时。                 └────────┘         └──────┘│     │
│  │                                   ┌────────┐  ←对抗→ ┌──────┐│     │
│  │  ❺稀疏延迟奖励归因：              │ 上下文  │         │推理  ││     │
│  │  84%算力浪费在非关键步骤，         │ 完整性  │         │成本  ││     │
│  │  轨迹级奖励太粗，归因困难。        └────────┘         └──────┘│     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌────────────────────┐ ┌────────────────────┐ ┌────────────────────┐│
│  │ 🟠f off-policy容忍  │ │ 🟠g 步级归因        │ │ 🟠h 上下文自压缩    ││
│  │ +多样性探索         │ │                    │ │                    ││
│  │                    │ │ Cursor实时RL       │ │ M级上下文推理开销   ││
│  │ Decoupled PPO     │ │ +Meta Self-Play    │ │ 爆炸，模型通过RL   ││
│  │ (Off-Policy基础)   │ │ SWE-RL             │ │ 学会自主压缩       ││
│  │ +DAPO (ByteDance) │ │                    │ │                    ││
│  │ +CISPO (MiniMax)  │ │ 极快闭环缩短       │ │ Composer 2:        ││
│  │                    │ │ horizon            │ │ 200K→1K,           ││
│  │ IS修正+探索鼓励    │ │ +自博弈无限数据    │ │ error降50%         ││
│  │ → 应对❹异步过时    │ │ +环境反馈过程信号  │ │ 不靠外部truncation ││
│  │                    │ │ → 应对❺归因太粗    │ │ 丢信息             ││
│  │ → P13 详细展开     │ │ → P14 详细展开     │ │ → Cursor self-     ││
│  │                    │ │                    │ │   summarization    ││
│  └────────────────────┘ └────────────────────┘ └────────────────────┘│
│                                                                     │
│  ▶ 算法层三个方向——先看off-policy容忍怎么做                           │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "框架层展开完毕，进入最上层——算法层。算法层由挑战❶❹❺共同驱动，承载三个升级方向。
>
> 挑战❶在基础设施层解决了显存和推理加速，但算法层还有一个维度——M级上下文推理开销爆炸，外部truncation会丢信息，模型必须学会自主压缩。
>
> 挑战❹探索效率。请看右上角对抗关系——吞吐量和新鲜度不可兼得。异步数据天然过时。
>
> 挑战❺稀疏延迟奖励归因。归因精度和效率不可兼得。84%的rollout算力浪费在非关键步骤上，轨迹级奖励太粗。
>
> 三个方向。第一，off-policy容忍+多样性探索——Decoupled PPO做off-policy基础，DAPO和CISPO做工业级改进，IS修正加探索鼓励应对❹。第二，步级归因——Cursor实时RL做极快闭环缩短horizon获取过程信号，Meta自博弈生成无限训练数据应对❺。第三，上下文自压缩——模型通过RL学会自主压缩上下文，Composer 2的self-summarization把200K token压到1K，压缩错误降50%，不靠外部truncation丢信息。
>
> 接下来两页逐一展开，其中P14同时覆盖步级归因和上下文自压缩。"

---

## P13 | DAPO+CISPO+Decoupled PPO（off-policy容忍）

### PPT内容

**标题**：Decoupled PPO → DAPO+CISPO：从Off-Policy基础到工业级异步RL
**标签**：🟠算法层 · 升级方向f · off-policy容忍+多样性探索 · 挑战❹驱动

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Decoupled PPO → DAPO+CISPO：Off-Policy基础到工业级异步RL      │
│  标签：🟠算法层 · 升级方向f · 挑战❹探索效率                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  【上半部】Off-Policy基础：Decoupled PPO                      │    │
│  │                                                             │    │
│  │  问题：异步架构下rollout数据天然off-policy                    │    │
│  │  - actor生成数据时用的是旧策略                               │    │
│  │  - learner更新时策略已经变了                                 │    │
│  │  - 策略版本差>4步直接发散(SkyRL实测)                          │    │
│  │  - 这是所有异步RL的基础问题                                  │    │
│  │                                                             │    │
│  │  方案：actor-learner物理分离 + V-trace截断IS修正              │    │
│  │        + importance weight裁剪                               │    │
│  │  → DAPO和CISPO都建立在这个基础之上                           │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌──────────────────────────────┐ ┌──────────────────────────────┐  │
│  │  DAPO (ByteDance Seed)       │ │  CISPO (MiniMax)             │  │
│  │                              │ │                              │  │
│  │  四大技术：                    │ │  对IS权重做更精细裁剪         │  │
│  │  · Clip-Higher 打破不对称裁剪 │ │  200K上下文+百万级日吞吐     │  │
│  │  · 动态采样 提升效率           │ │  比DAPO快2x                  │  │
│  │  · Token级梯度 精细化更新     │ │  驱动M2.5达SWE-Bench 80.2%  │  │
│  │  · 超长奖励整形 解决信号衰减  │ │                              │  │
│  │  AIME 50分(32B)              │ │  两者互补：                   │  │
│  │  已集成OpenRLHF              │ │  DAPO做精度，CISPO做规模      │  │
│  │  2026开源RL事实标准           │ │                              │  │
│  └──────────────────────────────┘ └──────────────────────────────┘  │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：三层递进——Decoupled PPO做off-policy地基，DAPO      │    │
│  │  解决裁剪/采样/梯度三大工业级问题，CISPO做大规模验证。          │    │
│  │  已成工业界共识，不是学术探索而是生产级验证。                   │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU的RL kernel需原生支持off-policy IS计算、V-trace     │    │
│  │  修正、token级梯度、动态采样过滤、IS权重裁剪等算子             │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "算法层第一个升级方向——off-policy容忍+多样性探索，三个技术层层递进。
>
> 先看基础——Decoupled PPO。异步架构下actor用旧策略生成数据，learner更新时策略已变，rollout数据天然off-policy。SkyRL实测策略版本差超过4步直接发散。解法是actor-learner物理分离加V-trace截断IS修正加importance weight裁剪。这是所有异步RL的地基。
>
> 在这个基础上，DAPO来自ByteDance Seed，提出四大技术——Clip-Higher打破不对称裁剪、动态采样、Token级梯度、超长奖励整形。32B模型AIME达50分，已集成OpenRLHF成为2026年开源RL事实标准。CISPO来自MiniMax，对IS权重做更精细裁剪，在200K上下文百万级日吞吐下验证，比DAPO快2倍，驱动M2.5达SWE-Bench 80.2%。
>
> 关键逻辑：三层递进——Decoupled PPO做地基、DAPO解决工业级问题、CISPO做大规模验证。已从学术探索走向工业共识。对我们的启示——NPU的RL kernel需原生支持off-policy IS计算、token级梯度、动态采样等算子。"

---

## P14 | Cursor实时RL + Meta SSR（步级归因 + 上下文自压缩）

### PPT内容

**标题**：Cursor实时RL + Meta SSR：极快闭环 × 自博弈 × 上下文自压缩
**标签**：🟠算法层 · 升级方向g步级归因+h上下文自压缩 · 挑战❶+❺驱动

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Cursor实时RL + Meta SSR：极快闭环 × 自博弈 × 上下文自压缩        │
│  标签：🟠算法层 · 升级方向g+h · 挑战❶长序列推理+❺稀疏奖励归因           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】两个问题                     │  │ 【右侧】两条互补路径 │ │
│  │                                      │  │                     │ │
│  │  问题1：                              │  │  ● Cursor实时RL     │ │
│  │  闭环太慢——传统RL训练闭环             │  │    Anyrun平台       │ │
│  │  cycle数小时甚至数天，                │  │    10万+沙箱环境     │ │
│  │  策略更新跟不上探索速度               │  │    5小时端到端闭环   │ │
│  │                                      │  │    日内多次部署到    │ │
│  │  问题2：                              │  │    数百万用户        │ │
│  │  数据太贵——Agent训练数据              │  │    → 极快闭环缩短   │ │
│  │  需要真实环境交互轨迹，               │  │      horizon        │ │
│  │  人工标注成本极高，                   │  │    密集闭环天然包含  │ │
│  │  规模受限                            │  │    环境反馈的过程    │ │
│  │                                      │  │    信号(步级归因)    │ │
│  │  问题3：                              │  │                     │ │
│  │  轨迹级奖励太粗——84%算力             │  │  ● Meta Self-Play   │ │
│  │  浪费在非关键步骤，无法区分            │  │    SWE-RL (SSR)     │ │
│  │  "碰巧做对"和"真正学会"              │  │    自博弈生成无限   │ │
│  │                                      │  │    训练数据          │ │
│  │  ┌────────────────────────┐          │  │    零人工标注        │ │
│  │  │ 瓶颈示意：              │          │  │    SWE-Bench         │ │
│  │  │ 传统：train→eval→fix   │          │  │    Verified +10.4分  │ │
│  │  │       [数小时/数天]     │          │  │    数据量上去后      │ │
│  │  │ Cursor: 5小时端到端    │          │  │    归因信号统计      │ │
│  │  └────────────────────────┘          │  │    质量自然提高      │ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  🟠h 上下文自压缩——self-summarization的双重意义              │    │
│  │                                                             │    │
│  │  Cursor Composer 2 的 self-summarization 既是步级归因(g)     │    │
│  │  的实证，也是上下文自压缩(h)的实证：                          │    │
│  │  · 步级归因维度：密集闭环让每步都有环境反馈→天然过程信号       │    │
│  │  · 上下文自压缩维度：模型通过RL学会自主压缩上下文              │    │
│  │    200K token → ~1K token，压缩错误↓50%                      │    │
│  │    不靠外部truncation丢信息，模型内生能力替代Harness规则       │    │
│  │  · 业界参考：Cursor self-summarization / ACON                │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：Cursor缩短horizon获取密集过程信号→步级归因(g)，     │    │
│  │  Meta扩展数据量提升归因统计质量。self-summarization同时验证了   │    │
│  │  上下文自压缩(h)——模型内化Harness能力的标志性案例。            │    │
│  │  三条路互补：一个做快、一个做大、一个做压缩                     │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU需支持高并发沙箱RL闭环能力、自博弈数据生成管线，      │    │
│  │  以及长上下文压缩推理的高效实现，这是Agent RL训练的基础能力     │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "算法层第二和第三个升级方向——步级归因加上下文自压缩。这一页同时覆盖两个方向，因为Cursor的self-summarization恰好是两者的交汇点。
>
> 先看步级归因。传统RL只在轨迹末端给一个奖励，84%算力浪费在非关键步骤上。Cursor的实时RL核心是极快闭环——Anyrun平台同时运行10万以上沙箱环境，5小时端到端RL闭环，日内多次部署到数百万用户。密集短循环让每步都有环境反馈的过程信号，天然实现步级归因。Meta的Self-Play SWE-RL核心是零人工标注的无限数据，SWE-Bench Verified提升10.4分。
>
> 再看上下文自压缩。这是算法层第三个方向，由挑战❶长序列推理开销在算法侧驱动。M级上下文推理开销爆炸，外部truncation丢信息。Composer 2的self-summarization同时验证了这个方向——模型通过RL学会自主压缩上下文，200K token压到约1K，压缩错误降50%。这不是工程截断，是模型内生能力替代Harness规则——正是P2讲的'Harness能力迁移到模型'的标志性案例。
>
> 关键逻辑：Cursor做快获取步级信号，Meta做大提升归因质量，self-summarization做压缩解决长上下文推理爆炸。三条路互补。
>
> 至此三层架构全部展开——基础设施层3个方向、框架层2个、算法层3个，加横切约束穿透全部三层。最后一页收束定位和行动。"

---

## P15 | 收束层：全栈协同 — 对我们团队的启示

### PPT内容

**标题**：全栈协同：三层架构八大升级 x R3数据流 x 6挑战全覆盖

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：全栈协同：三层架构八大升级 × R3数据流 × 6挑战全覆盖              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  核心判断（大字，居中，高亮背景）：                              │    │
│  │  6挑战+横切约束沿R3数据流穿透三层架构                         │    │
│  │  三层八大升级 × 全栈协同 = 我们"帮客户开箱即用"的价值锚点       │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 🔵行动方向 ①      │ │ 🟢行动方向 ②      │ │ 🟠行动方向 ③      │    │
│  │ Reply：           │ │ Routing：         │ │ Record：          │    │
│  │ 训推协同基础设施   │ │ 有状态推理引擎     │ │ 可控探索平台       │    │
│  │                  │ │                  │ │                  │    │
│  │ 对应挑战：        │ │ 对应挑战：        │ │ 对应挑战：        │    │
│  │ C1 策略时效性     │ │ C2 采集吞吐       │ │ C6 环境保真       │    │
│  │ C4 奖励与归因     │ │ C3 推理效率       │ │ C7 工程可达       │    │
│  │                  │ │                  │ │                  │    │
│  │ 关键能力：        │ │ 关键能力：        │ │ 关键能力：        │    │
│  │ · 异步训练调度    │ │ · Rollout解耦    │ │ · sandbox保真    │    │
│  │ · 跨节点通信优化  │ │ · KV cache 共享  │ │ · 可控探索平台   │    │
│  │ · 策略版本管理    │ │   /迁移能力      │ │ · 框架级分层解耦 │    │
│  │ · step-level归因 │ │ · 互联架构优化    │ │ · 大规模环境编排 │    │
│  │ · 闭环训练加速    │ │   (HCCS)        │ │ · 精度一致性     │    │
│  │ · off-policy IS  │ │ · 多级内存管理    │ │   (天然优势)     │    │
│  │ · 上下文自压缩    │ │                  │ │                  │    │
│  │ 技术参考：        │ │                  │ │ 技术参考：        │    │
│  │ Decoupled PPO /  │ │ 技术参考：        │ │ Forge分层设计     │    │
│  │ DAPO+CISPO /     │ │ ProRL Agent /    │ │ ⚠️ 行业空白=     │    │
│  │ Cursor+Meta SSR  │ │ Dynamo / LMCache │ │   我们的切入机会  │    │
│  │ /self-summariz.  │ │ / Mooncake       │ │                  │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  ⚪横切约束 C5（数值可信）── 穿透三个行动方向的共同底线         │    │
│  │  NPU精度+训推一致性 = 全栈协同的基础                            │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  收束（大字，高亮条）：                                        │    │
│  │  Agent 时代的竞争，不在谁的模型更大，而在谁的全栈更顺。         │    │
│  │  我们的机会：让 NPU 成为客户做 Agentic RL 后训练的最优选择。   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "最后收束。六个挑战加横切约束，沿R3数据流穿透三层八大升级（3-2-3）。但今天汇报最核心的判断不只是'需要全栈协同'——而是**行业空白在哪里，我们该卡在哪里**。
>
> 请看三个行动方向。
>
> 行动方向一：Reply环节——训推协同基础设施，对应C1策略时效性和C4奖励与归因。关键能力是异步训练调度、跨节点通信优化、策略版本管理，以及off-policy IS修正。参考 Decoupled PPO、DAPO+CISPO、Cursor实时RL+Meta SSR。
>
> 行动方向二：Routing环节——有状态推理引擎，对应C2采集吞吐和C3推理效率。关键能力是 Rollout解耦、KV cache 共享和迁移、HCCS 互联架构优化、多级内存管理。参考 ProRL Agent、Dynamo、LMCache、Mooncake——这一层技术最密集，也是我们推理引擎团队最有基础快速跟进的。
>
> 行动方向三：Record环节——**可控探索平台，这是我们的差异化主战场**。对应C6环境保真和C7工程可达。目前业界只有Forge一个方案，sandbox保真和可控探索仍是行业空白。作为硬件平台方，我们在精度一致性、sandbox保真度、大规模环境编排方面有天然优势——这不是追赶，是卡位。
>
> 请注意底部这行——横切约束C5数值可信，穿透三个行动方向的共同底线。NPU上的算子精度和训推一致性是全栈协同的基础。如果底层数值不可信，上层所有优化都建立在沙滩上。这也是我们作为硬件平台方独有的优势。
>
> 特别强调——这次技术参考全部来自工业巨头的生产级实践：ByteDance的DAPO、MiniMax的CISPO和Forge、NVIDIA的ProRL Agent、Cursor和Meta的闭环训练。不是学术论文展望，是已经在线上跑、有真实数据验证的方案。
>
> 910C 和 H100 的裸性能差距是 60%，但如果我们在精度和一致性上做到业界最好——确定性计算、批不变性、训推一致性——客户会因为训练结果可复现而选择我们，而不仅仅因为 FLOPS 便宜。
>
> 底线判断：**Agent 时代的竞争，不在谁的模型更大，而在谁的全栈更顺。三层架构八大升级（3-2-3）沿R3数据流覆盖全部六个挑战加横切，这就是我们的全景作战图。我们的机会是让 NPU 成为客户做 Agentic RL 后训练的最优选择——不是 GPU 的替代品，而是全栈优化的差异化平台。**
>
> 今天汇报的核心诉求：Agent RL 后训练是确定性的算力需求方向，我们的技术布局已经对准了R3数据流上的六大挑战，需要资源支持加速落地。谢谢各位领导。"

---

## 附：页码总览与逻辑流

```
P1  Agent时代已来                              ← 定调：产品已落地+开源引爆+巨头真金白银
P2  演进+不可替代性（合并）                      ← 动因：三阶段演进+排除法+桥接层+飞轮+巨头验证
P3  七维→五挑战+横切（直接展示）                 ← 推导：七维对比→5挑战直接正推+横切
P4  五挑战→八升级方向（三列因果流）+架构锚      ← 导航：三列因果流+底部三层架构锚条
P5  🔵基础设施层概览（挑战❶+❷驱动）             ← KV池化+推理加速+sandbox并行（3个方向）
P6  LMCache+Mooncake                           ← 🔵a KV Cache池化（节点孤岛→集群级KV池）
P7  Eagle3+4bit量化                            ← 🔵b 推理加速（投机推理×量化，70B成本可控）
P8  Forge                                      ← 🔵c sandbox并行化（40x加速+行业空白）
P9  🟢框架层概览（挑战❷+❸驱动）                 ← 训推解耦+异步调度（2个方向）
P10 ProRL Agent                                ← 🟢d 训推解耦（NVIDIA，Rollout-as-a-Service）
P11 Dynamo                                     ← 🟢e 异步调度（KV感知路由+动态调度）
P12 🟠算法层概览（挑战❶+❹+❺驱动）               ← off-policy容忍+步级归因+上下文自压缩（3个方向）
P13 Decoupled PPO→DAPO+CISPO                   ← 🟠f off-policy容忍（三层递进）
P14 Cursor实时RL+Meta SSR                      ← 🟠g 步级归因+🟠h 上下文自压缩（极快闭环×自博弈×自压缩）
P15 全栈协同·团队启示                            ← 收束：三层八大升级(3-2-3)+横切=我们的卡位机会
```

**页间逻辑链**：现象(P1) → 论证why(P2) → 推导5挑战+横切(P3) → 三列因果流+架构锚(P4) → 自底向上逐层展开：🔵基础设施(P5-P8) → 🟢框架(P9-P11) → 🟠算法(P12-P14) → 收束行动(P15)

**五挑战→八升级方向→三层映射（v9更新：+h上下文自压缩）**：
- 🔵基础设施层（挑战❶+❷驱动/3个方向）：a)KV Cache池化 b)推理加速 c)sandbox并行化 → P5概览+P6-P8逐一展开
  - LMCache/Mooncake、Eagle3/4bit量化、Forge
- 🟢框架层（挑战❷+❸驱动/2个方向）：d)训推解耦 e)异步调度 → P9概览+P10-P11逐一展开
  - ProRL Agent、Dynamo
- 🟠算法层（挑战❶+❹+❺驱动/3个方向）：f)off-policy容忍+多样性探索 g)步级归因 h)上下文自压缩 → P12概览+P13-P14逐一展开
  - Decoupled PPO/DAPO+CISPO、Cursor实时RL+Meta SSR、Cursor self-summarization/ACON
- 横切（数值精度保证）：训推一致性/确定性计算/精度基线/算子自动生成

**五挑战体系（v7更新）**：
1. ❶ 长序列推理开销（维度①+⑦）
2. ❷ 环境交互延迟（维度②+③）
3. ❸ 在线数据吞吐瓶颈（维度④）
4. ❹ 探索效率（维度⑥）
5. ❺ 稀疏延迟奖励的归因（维度⑤）
+ 横切：数值可信

**数据校准说明**：
- GitHub 星数：OpenClaw 24.7万（原始PPT数据）
- Anthropic RL 环境投入：表述为"讨论年投$1B+"（The Information 报道，非官方确认）
- 后训练算力 40%+：指广义后训练（含 RLHF+蒸馏+合成数据等）
- SFT 天花板：已修正为"训练分布≠部署分布 (distribution shift)"（P2P3-final.md 三方共识）
- 对比维度：七维（含"上下文"维度）
- 挑战体系：从6挑战+1横切(C1-C7)简化为5挑战+横切，按维度正推
- 升级方向：8个方向按因果链（因为→所以→否则）组织，按三层架构分组（3-2-3）
- 技术分布：基础设施层3个方向、框架层2个方向、算法层3个方向（含h上下文自压缩）+横切
