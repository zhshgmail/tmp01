# PPT + 演讲稿 最终版

> 主题：Agent 时代下，Agentic RL 后训练的核心挑战与全栈应对
> 受众：洞察分享会（华为计算部门高级领导）
> 风格：华为/科技企业风格，深色背景
> 总计：15页，每页配60-90秒演讲话术

---

## P1 | 定调层：Agent 时代已来

### PPT内容

**标题**：智能涌现：Agent 从概念验证走向规模落地，AI 产业进入"做事"时代

**核心信息**：产品、开源、资本三个维度同时爆发——Agent 时代不是预测，是正在发生的现实。

```
┌─────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                     │
│  标题：智能涌现：Agent 从概念验证走向规模落地                            │
│  副标题：AI 产业进入"做事"时代                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────┐        │
│  │            顶部：一句话定调（大字，加粗，居中）              │        │
│  │  "从'能聊'到'能做'的分水岭已经跨过，Agent 是载体"           │        │
│  └─────────────────────────────────────────────────────────┘        │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 产品已落地         │ │ 开源正在引爆      │ │ 巨头真金白银      │    │
│  │  ——加速时间线      │ │                  │ │                  │    │
│  │                  │ │ ● Claude Code    │ │ ● Anthropic      │    │
│  │ 2024.3 Devin     │ │   代码泄露事件    │ │   讨论年投$1B+   │    │
│  │  编程Agent,端到端 │ │   (2026.3 npm    │ │   采购RL环境     │    │
│  │  开发             │ │    sourcemap     │ │   (The Info.)    │    │
│  │                  │ │    意外暴露51万行 │ │                  │    │
│  │ 2025.1 Operator  │ │    源码)         │ │ ● OpenAI         │    │
│  │  GUI Agent       │ │                  │ │   RFT 对o4-mini  │    │
│  │  (后升级为       │ │ ● OpenClaw       │ │   公开发布       │    │
│  │   Agent Mode)    │ │   基于泄露代码    │ │   (2025.5)       │    │
│  │                  │ │   社区重写的通用  │ │                  │    │
│  │ 2025.3 Manus     │ │   Agent框架      │ │ ● Google         │    │
│  │  通用Agent,多步骤│ │   一天10万星     │ │   Gemini 2.5整合 │    │
│  │  自主完成        │ │   24.7万GitHub星 │ │   Agent RL训练   │    │
│  │                  │ │   集成20+平台    │ │   多步骤+工具    │    │
│  │ 2025.7           │ │                  │ │                  │    │
│  │  ChatGPT Agent   │ │ ● 开源小模型     │ │ ● Microsoft      │    │
│  │  Mode（替代      │ │   在Agent场景    │ │   Copilot→Agent  │    │
│  │  Operator）      │ │   追平大模型     │ │   编排平台       │    │
│  │                  │ │                  │ │                  │    │
│  │ 2025.10          │ │                  │ │                  │    │
│  │  Claude Computer │ │                  │ │                  │    │
│  │  Use GA          │ │                  │ │                  │    │
│  │                  │ │                  │ │                  │    │
│  │  ↓ 节奏在加快     │ │                  │ │                  │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│          ↓                    ↓                    ↓                │
│  ┌─────────────────────────────────────────────────────────┐        │
│  │ 底部汇聚条：产品从早期探索走向成熟，开源社区用脚投票，       │        │
│  │ 巨头用真金白银下注 → Agent 时代已来                        │        │
│  └─────────────────────────────────────────────────────────┘        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**内容要素**：

- **左列：产品已落地**（时间线呈现加速节奏）
  - 2024.3 Devin：编程 Agent，端到端完成开发任务
  - 2025.1 Operator → 2025.7 ChatGPT Agent Mode
  - 2025.3 Manus：通用 Agent，多步骤任务自主完成
  - 2025.10 Claude Computer Use GA：桌面级 Agent 正式版
- **中列：开源正在引爆**
  - Claude Code 代码泄露：2026.3 npm sourcemap 意外暴露 51.2 万行源码
  - OpenClaw：社区基于泄露代码重写的通用 Agent 框架，24.7 万 GitHub 星
  - 开源小模型在 Agent 场景追平甚至超越大模型
- **右列：巨头真金白银**
  - Anthropic：讨论年投 $1B+ 采购 RL 环境（The Information 报道）
  - OpenAI：RFT 2025.5 对 o4-mini 公开发布
  - Google：Gemini 2.5 训练流程中 RL 阶段支持多步骤动作+工具调用
  - Microsoft：Copilot 从"副驾驶"升级为 Agent 编排平台

### 演讲话术（60-90秒）

> "各位领导，今天不讲趋势综述，直接说结论：Agent 已经从概念验证进入规模落地，产品、开源、资本三条线在 2026 年 Q1 同时爆发。
>
> 先看产品端。请看左列时间线——从 2024 年 Devin 做概念验证，到 2025 年 ChatGPT Agent Mode 替代 Operator，再到 Claude Computer Use 正式版发布，节奏在明显加快。特别是 Cursor Composer 2，一家 IDE 公司用 RL 训练自研模型打败了 Claude Opus 4.6——这说明 Agent RL 已经是产品竞争力的核心。
>
> 再看开源端。中间这列——Claude Code 源码泄露后，社区基于它重写了 OpenClaw 框架，24.7 万 GitHub 星。DeepSWE 用 RL 训练开源 32B 模型达到 Pass@1 42.2%。开源做 Agent 的门槛已经降到可触达。
>
> 最后看资本端。右列——据 The Information 报道，Anthropic 领导层讨论年投超 10 亿美元采购 RL 训练环境。广义后训练算力占总算力份额持续攀升到约 40%。三家路线不同，但共识一致。
>
> 这三条线同时爆发意味着：Agent 后训练不再是实验室项目，而是真实的算力负载——我们的 NPU 平台必须能承接这类负载。这是今天汇报的出发点。"

---

## P2 | 动因层：后训练演进与 Agentic RL 的不可替代性

### PPT内容

**标题**：不可替代：从 SFT 到 Agentic RL，Agent 能力提升只有一条路

```
┌──────────────────────────────────────────────────┐
│ [深色渐变头部条]                                    │
│ 标题：不可替代：从 SFT 到 Agentic RL，             │
│       Agent 能力提升只有一条路                      │
├──────────────────────────────────────────────────┤
│                                                  │
│ 【上半页：三阶段演进横向箭头 + 排除法】【原有+微调】  │
│                                                  │
│ ┌──────────────┐  →  ┌──────────────┐  →  ┌──────────────┐
│ │ 第一阶段       │     │ 第二阶段       │     │ 第三阶段 ★    │
│ │ SFT / RLHF    │     │ DPO / KTO     │     │ Agentic RL   │
│ │               │     │               │     │              │
│ │ 模型：GPT-4   │     │ 模型：Llama3.3│     │ 模型：        │
│ │ Llama-2-Chat  │     │ Qwen-2.5     │     │ GPT-5.4      │
│ │ ChatGLM-3     │     │ Mistral Lg.2 │     │ Claude Op4.6 │
│ │               │     │               │     │ Gemini 3.1Pro│
│ │ 算力：<5%     │     │ 算力：5-15%   │     │ DeepSeek-V3.2│
│ │ 场景：3-5种   │     │ 场景：5-10种  │     │ Llama4 Mav.  │
│ │ (对话/问答)   │     │ (+偏好/安全)  │     │ GLM-5        │
│ │               │     │               │     │              │
│ │ ┌──────────┐ │     │ ┌──────────┐ │     │ 算力：40%+   │
│ │ │✗ 天花板   │ │     │ │✗ 天花板   │ │     │ 场景：50+种  │
│ │ │状态空间大 │ │     │ │奖励来自  │ │     │              │
│ │ │演示覆盖  │ │     │ │环境不来  │ │     │ ┌──────────┐ │
│ │ │不了；    │ │     │ │自偏好；  │ │     │ │✓ 唯一路径 │ │
│ │ │训练分布  │ │     │ │能教对齐  │ │     │ │环境反馈   │ │
│ │ │≠部署分布 │ │     │ │不能教交互│ │     │ │覆盖无限态 │ │
│ │ │(dist.    │ │     │ └──────────┘ │     │ │从错误中学 │ │
│ │ │ shift)   │ │     │              │     │ └──────────┘ │
│ │ └──────────┘ │     └──────────────┘     │              │
│ └──────────────┘                          └──────────────┘
│                                                  │
│ 中间一句话（高亮）：                                │
│ 后训练算力<5%→40%+，Agent必须在做事中学会做事       │
│                                                  │
│ ┌────────────────────────────────────────────────┐│
│ │【桥接层——为什么RL是"唯一闭环方法"】               ││
│ │                                                ││
│ │ 金句（高亮）：                                   ││
│ │ "Harness的能力图，就是未来模型后训练的负载"       ││
│ │                                                ││
│ │ 公式：Agent能力 = 模型 + Harness               ││
│ │ 方向：Harness今天做的 → 模型明天要内化的         ││
│ │ 唯一性：SFT/DPO教静态映射；                      ││
│ │         RL = 唯一能在环境闭环中吸收能力的方法     ││
│ │                                                ││
│ │ 迁移实证（仅保留一行）：                          ││
│ │ ┌──────────────────────────────────────────┐   ││
│ │ │ 上下文压缩：Harness侧规则 → 模型内生能力  │   ││
│ │ │ Cursor Composer 2 用RL训self-summarization│   ││
│ │ │ 压缩错误↓50%，5000+token → ~1000token     │   ││
│ │ └──────────────────────────────────────────┘   ││
│ └────────────────────────────────────────────────┘│
│                                                  │
│ 【下半页：飞轮 + 巨头验证】                         │
│                                                  │
│    ┌──────────┐                                  │
│    │ 模型更强  │←────────────────┐                │
│    └────┬─────┘                 │                │
│         ↓                       │                │
│  ┌────────────┐          ┌──────┴─────┐          │
│  │在环境中做事 │          │更新模型参数  │          │
│  │(Harness)   │          │            │          │
│  └──────┬─────┘          └──────┬─────┘          │
│         │                       ↑                │
│   Anthropic                Google                │
│   $1B+采购环境             Gemini全流程RL         │
│         │                       ↑                │
│         ↓                       │                │
│  ┌────────────┐          ┌──────┴─────┐          │
│  │产生交互轨迹 │─────────→│得到环境反馈  │          │
│  └────────────┘          └────────────┘          │
│                                ↑                  │
│                           OpenAI                  │
│                           RFT开放grader           │
│                                                  │
│  三家全部押注RL后训练，没有一家认为SFT够用          │
│                                                  │
│ ┌──────────────────────────────────────────────┐ │
│ │ 结论（高亮）：                                  │ │
│ │ Agentic RL后训练 = Agent能力提升的不可替代路径  │ │
│ │ → 但直接把LLM RL搬到Agent场景，会结构性失配    │ │
│ │ → 接下来看：七维差异沿R3数据流催生哪六大挑战？  │ │
│ └──────────────────────────────────────────────┘ │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "P1 我们看到 Agent 时代已经来了。那凭什么说 Agentic RL 后训练是关键？这一页用排除法回答。
>
> 请看上半页三个阶段。第一代 SFT/RLHF，天花板是训练分布不等于部署分布——Agent 的状态空间太大，演示覆盖不了。第二代 DPO/KTO，天花板是奖励来自环境验证而非人类偏好——能教对齐不能教交互。排除完前两条路，只剩 Agentic RL：环境反馈覆盖无限状态，从错误中学习。
>
> 排除法告诉我们只剩 RL 一条路，但**为什么 RL 就能行**？
>
> 一个关键洞察：Agent 的能力等于模型加上 Harness——就是围绕模型的工程脚手架。今天 Harness 做的上下文压缩、工具编排、错误恢复，明天都要被模型内化。换句话说，**Harness 的能力图，就是未来模型后训练的负载**。
>
> SFT 只能教静态输入输出映射，DPO 只能教偏好排序，它们都无法让模型在做事中学会做事。RL 是唯一能通过环境闭环让模型吸收这些动态能力的方法。
>
> 一个已经落地的实证：Cursor Composer 2 用 RL 训练 self-summarization，把上下文压缩从 Harness 侧的工程方案变成模型的内生能力，压缩错误降了 50%，摘要长度从 5000 多 token 压到 1000 左右。
>
> 再看下半页飞轮——Anthropic 投环境、OpenAI 开放 grader、Google 做全流程 RL。三家全部押注 RL 后训练，没有一家认为 SFT 够用。
>
> 所以结论很清楚：Agentic RL 后训练是 Agent 能力提升的不可替代路径。但——直接把传统 LLM RL 搬到 Agent 场景，会结构性失配。接下来我们看，到底哪里不同。"

---

## P3 | 七维变化 → 六子挑战+横切（R3分组）

### PPT内容

**标题**：范式跃迁：七维结构性变化催生六大子挑战，沿R3数据流分布

**核心信息**：左半页用七维对比表展示差异并显式分三组，右半页按R3数据流（Reply/Routing/Record）分组展示6个细挑战+1个横切约束，每个挑战标注来源维度组、色标区分、量化严重性。逻辑从"变化→R3环节→挑战"，因果直接且结构化。

**P2→P3 承接**：P2 论证了"必须走 Agentic RL"，P3 回答"Agentic RL 和 LLM RL 到底有什么不同？这些不同沿R3数据流直接造成哪六大挑战？"

```
┌──────────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                          │
│  标题：范式跃迁：七维变化 → R3×三层 → 六子挑战+横切                          │
├────────────────────────────────┬─────────────────────────────────────────┤
│                                │                                         │
│  【左半页 40%】                 │  【右半页 60%】                           │
│  LLM RL vs Agentic RL         │  6子挑战+1横切，按R3分组                  │
│  七维对比表 + 显式分组          │  色标区分+量化严重性                       │
│                                │                                         │
│  ┌──────────────────────────┐  │  ┌─ 🔵蓝色区 Reply（算法层）──────┐    │
│  │ 七维对比表（分组+箭头）    │  │  │                                  │   │
│  │                          │  │  │  C1 策略时效性 ← 组A（算力+循环） │   │
│  │ 维度    │LLM RL │Agntc RL│  │  │  异步数据天然过时，策略版本差      │   │
│  │ ────────┼───────┼────────│  │  │  >4步训练发散（SkyRL数据）        │   │
│  │                          │  │  │                                  │   │
│  │ ┌─ 组A（🔵蓝色）────────┐ │  │  │  C4 奖励与归因 ← 组C（奖励+探索）│   │
│  │ │ 算力  │训练为主│推理90% │ │  │  │  84%rollout算力浪费在非关键步骤  │   │
│  │ │ 循环  │开环    │闭环    │ │──│──→ （CSO 16%反推）                │   │
│  │ └───────────────────── ┘ │  │  └──────────────────────────────────┘   │
│  │     ↓ 推出 C1,C4          │  │                                         │
│  │                          │  │  ┌─ 🟢绿色区 Routing（框架层）────┐    │
│  │ ┌─ 组B（🟢绿色）────────┐ │  │  │                                  │   │
│  │ │ 环境  │无      │真实   │ │  │  │  C2 采集吞吐 ← 组B               │   │
│  │ │ 数据  │标注    │在线轨迹│ │  │  │  （环境+数据+上下文）             │   │
│  │ │ 上下文│短      │M级长程 │ │──│──→ 单Agent会话KV Cache数GB，        │   │
│  │ └───────────────────── ┘ │  │  │  千并发撑爆显存                    │   │
│  │     ↓ 推出 C2,C3          │  │  │                                  │   │
│  │                          │  │  │  C3 推理效率 ← 组A+B（算力+上下文）│   │
│  │ ┌─ 组C（🟠橙色）────────┐ │  │  │  70B模型不量化单卡放不下          │   │
│  │ │ 奖励  │人评    │环境验证│ │  │  └──────────────────────────────────┘   │
│  │ │ 探索  │KL约束  │大探索  │ │──│──→                                     │
│  │ └───────────────────── ┘ │  │  ┌─ 🟠橙色区 Record（基础设施层）──┐    │
│  │     ↓ 推出 C6,C7          │  │  │                                  │   │
│  │                          │  │  │  C6 环境保真 ← 组B（环境+数据）   │   │
│  └──────────────────────────┘  │  │  sandbox偏差导致训练信号失真       │   │
│                                │  │                                  │   │
│  注：组A→C1,C4；              │  │  C7 工程可达 ← 全维度共同推高     │   │
│  组B→C2,C3,C6；              │  │  算法2-4周迭代，算子适配3-6个月   │   │
│  组C→C4；                    │  └──────────────────────────────────┘   │
│  维度交叉共同驱动6挑战        │                                         │
│                                │  ┌─ ⚪横切 C5 数值可信 ────────────┐   │
│                                │  │  穿透R3全流程                     │   │
│                                │  │  RL对数值噪声极敏感，调参变玄学    │   │
│                                │  └──────────────────────────────────┘   │
│                                │                                         │
├────────────────────────────────┴─────────────────────────────────────────┤
│  【底部过渡条（高亮）】                                                     │
│  6子挑战+1横切沿R3数据流——Reply→Routing→Record——                          │
│  穿透算法层→框架层→基础设施层。下一页展开全栈需求传导。                       │
└──────────────────────────────────────────────────────────────────────────┘
```

**视觉设计要点**：
- 右半页按R3三环节分区：Reply蓝色区（C1+C4）、Routing绿色区（C2+C3）、Record橙色区（C6+C7）
- 横切C5用白色/灰色边框横跨底部，表示穿透全流程
- 左半页对比表三组用对应色标（组A蓝、组B绿、组C橙），箭头指向右侧对应子挑战
- 每个子挑战标注来源维度组和量化严重性数据
- 注意维度组与挑战的交叉映射关系（如组A推C1但也参与C3，组C推C4但C4归入Reply）

### 演讲话术（60-90秒）

> "上一页我们说，直接把LLM RL搬到Agent场景会结构性失配。失配在哪？我把七个维度摊开来看，推导出六个子挑战加一个横切约束。
>
> 左边这张表，七个维度分三组。但注意——维度组和挑战不是一对一的，而是交叉驱动。右半页按R3数据流分三个区。
>
> 蓝色区，Reply算法层，两个挑战。C1策略时效性——组A的算力和循环维度推出，90%算力在推理、闭环Episode分钟级，训推必须异步，数据天然过时。SkyRL数据显示策略版本差超过4步直接发散。C4奖励与归因——组C的奖励和探索维度推出，84%的rollout算力浪费在非关键步骤上。
>
> 绿色区，Routing框架层，两个挑战。C2采集吞吐——组B的环境、数据、上下文维度推出，单Agent会话KV Cache数GB，千并发撑爆显存。C3推理效率——组A和组B交叉驱动，70B模型不量化单卡放不下。
>
> 橙色区，Record基础设施层，两个挑战。C6环境保真——组B推出，sandbox偏差导致训练信号失真。C7工程可达——全维度共同推高，算法2到4周迭代，但算子适配要3到6个月。
>
> 最后，横切约束C5——数值可信——穿透R3全流程。RL对数值噪声极敏感，底层精度不可信，上层调参就变玄学。
>
> 底部这句话：六子挑战加横切，沿R3数据流穿透三层架构。下一页展开全栈需求传导。"

---

## P4 | Agent RL后训练的全栈需求传导

### PPT内容

**标题**：Agent RL后训练的全栈需求传导

**核心信息**：上半页用"R3环节×6子挑战+1横切→栈层→新要求"完整传导表，展示P3推导出的六大子挑战如何沿R3数据流逐层传导为具体技术需求；下半页将9项技术按三个栈层分列排布，每个技术标注它回应的具体子挑战。

**P3→P4 承接**：P3 推导出六子挑战+横切，P4 回答"这些挑战落在谁头上？业界给出了哪些回应？"

```
┌──────────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                          │
│  标题：Agent RL后训练的全栈需求传导                                         │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  【上半页：R3×6子挑战+横切→需求传导表】                                     │
│                                                                          │
│  ┌────────────┬──────────────────────────────┬───────────┬──────────────┐│
│  │  R3环节     │ 子挑战                        │ 落在哪层   │ 新要求        ││
│  ├────────────┼──────────────────────────────┼───────────┼──────────────┤│
│  │ 🔵Reply    │ C1 策略时效性                 │ 算法层     │ off-policy   ││
│  │ 回放更新    │   异步数据过时→训练发散         │           │ 修正+保鲜期  ││
│  │            ├──────────────────────────────┤           ├──────────────┤│
│  │            │ C4 奖励与归因                 │           │ step-level   ││
│  │            │   84%算力浪费在非关键步骤       │           │ 归因+时序分配 ││
│  ├────────────┼──────────────────────────────┼───────────┼──────────────┤│
│  │ 🟢Routing  │ C2 采集吞吐                   │ 框架层     │ KV-aware路由 ││
│  │ 路由请求    │   千并发KV Cache撑爆显存        │           │ +有状态管理  ││
│  │            ├──────────────────────────────┤           ├──────────────┤│
│  │            │ C3 推理效率                   │           │ 模型量化+    ││
│  │            │   70B不量化单卡放不下           │           │ 分离架构     ││
│  ├────────────┼──────────────────────────────┼───────────┼──────────────┤│
│  │ 🟠Record   │ C6 环境保真                   │ 基础设施层 │ sandbox保真  ││
│  │ 记录轨迹    │   sandbox偏差→训练信号失真      │           │ +完整轨迹    ││
│  │            ├──────────────────────────────┤           ├──────────────┤│
│  │            │ C7 工程可达                   │           │ 算子适配+    ││
│  │            │   算法迭代快，算子适配慢         │           │ harness质量  ││
│  ├────────────┴──────────────────────────────┴───────────┴──────────────┤│
│  │ ⚪横切约束 C5（数值可信）── 穿透R3全流程，每层都须守住精度底线             ││
│  └───────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  ── 结论条（高亮）──  Agent RL后训练不只是调算法，是全栈问题                  │
│                                                                          │
│  【下半页：三层 × 九项技术（按栈层归类）】                                    │
│                                                                          │
│  🔵算法层（Reply）         🟢框架层（Routing）         🟠基础设施层（Record）  │
│  ┌──────────────────┐    ┌──────────────────┐     ┌──────────────────┐  │
│  │ PRIME-RL          │    │ Dynamo            │     │ SALT              │  │
│  │ 全异步编排         │    │ KV-aware          │     │ 时序归因           │  │
│  │ →应对C1           │    │ 弹性路由           │     │ →应对C4           │  │
│  │                   │    │ →应对C2           │     │                   │  │
│  ├──────────────────┤    ├──────────────────┤     ├──────────────────┤  │
│  │ SkyRL             │    │ LMCache           │     │ SWE-PRM           │  │
│  │ 保鲜期过滤         │    │ KV Cache          │     │ 过程奖励           │  │
│  │ →应对C1           │    │ 跨请求复用         │     │ 建模              │  │
│  │                   │    │ →应对C2,C3        │     │ →应对C6           │  │
│  ├──────────────────┤    ├──────────────────┤     ├──────────────────┤  │
│  │ Off-Policy PPO    │    │ Mooncake          │     │ HiPER             │  │
│  │ off-policy        │    │ KV Cache分离      │     │ 分层探索           │  │
│  │ 重要性采样修正     │    │ 跨节点池化         │     │ 策略              │  │
│  │ →应对C1           │    │ →应对C2,C3        │     │ →应对C7           │  │
│  └──────────────────┘    └──────────────────┘     └──────────────────┘  │
│                                                                          │
│  底部：→ 接下来按R3数据流逐层展开，先看Reply环节                             │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  底部（高亮）：                                                            │
│  6子挑战+横切覆盖R3全流程 → 三层架构调整×全栈协同是唯一出路                   │
└──────────────────────────────────────────────────────────────────────────┘
```

**视觉设计要点**：
- 上半页传导表按R3环节分三段，每段含2个子挑战，底部横切C5单独一行
- 色标与P3一致：Reply蓝色、Routing绿色、Record橙色、横切白色/灰色
- 下半页九个技术框按三列排列，列标题标注R3环节（Reply/Routing/Record）
- 每个技术框新增"应对Cx"标注，明确技术与子挑战的对应关系
- 传导表和技术框之间用结论条"Agent RL后训练不只是调算法，是全栈问题"分隔

### 演讲话术（60-90秒）

> "P3我们推导出六个子挑战加一个横切约束。现在关键问题是：**这些挑战落在谁头上？**
>
> 注意看这张传导表——左列是R3数据流，每个环节对应两个子挑战。Reply回放更新是算法层的事，对应C1策略时效性和C4奖励与归因；Routing路由请求是框架层的事，对应C2采集吞吐和C3推理效率；Record记录轨迹是基础设施层的事，对应C6环境保真和C7工程可达。
>
> 再看最后一行：横切约束C5——数值可信——穿透R3全流程，每层都须守住精度底线。
>
> 这就是我想传达的核心判断：**Agent RL后训练不只是调算法，它是一个全栈问题。** 六个子挑战恰好两两卡在R3的三个环节上，每一层都被提出了新的要求。
>
> 下面九个技术框就是业界对这些新要求的回应，每个框都标注了它应对的具体子挑战。算法层三个技术——PRIME-RL、SkyRL、Off-Policy PPO——全部应对C1策略时效性，因为异步回放是Reply环节最紧迫的痛点。框架层——Dynamo、LMCache、Mooncake——应对C2和C3，解决推理侧的有状态吞吐难题。基础设施层——SALT、SWE-PRM、HiPER——分别应对C4归因、C6环境保真、C7工程可达。
>
> 这对我们计算部门意味着什么？意味着我们不能只看算法论文——推理引擎、调度系统、缓存架构全都要跟着变。接下来按R3数据流逐层展开，先从Reply环节开始。"

---

## P5 | Reply环节：策略时效性与奖励归因（C1+C4）

### PPT内容

**标题**：Reply环节：策略时效性与奖励归因（C1+C4）— 异步训练的核心张力

```
┌─────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                     │
│  标题：Reply环节：策略时效性与奖励归因（C1+C4）                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区（左侧 60%）             张力示意（右侧 40%）      │     │
│  │                                                            │     │
│  │  训推分离让推理集群全速跑        ┌────────┐  ←对抗→ ┌──────┐│     │
│  │  rollout，吞吐量上去了。         │ 吞吐量  │         │新鲜度││     │
│  │                                 │ ↑↑↑↑   │         │ ↑↑↑ ││     │
│  │  但 on-policy RL 要求数据       └────────┘         └──────┘│     │
│  │  来自最新策略。异步意味着                                    │     │
│  │  rollout 生成时用的参数，        异步架构里只能做权衡         │     │
│  │  等训练拿到时已经过期了。        不能兼得                     │     │
│  │                                                            │     │
│  │  Agent episode 动辄几分钟，                                 │     │
│  │  比 chatbot 长 1-2 个量级，                                │     │
│  │  陈旧度被放大。                                             │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 系统架构层         │ │ 训练框架层        │ │ 训练算法层        │    │
│  │                  │ │                  │ │                  │    │
│  │ PRIME-RL         │ │ SkyRL            │ │ Off-Policy PPO             │    │
│  │ PrimeIntellect   │ │ UC Berkeley      │ │ ByteDance/清华   │    │
│  │                  │ │                  │ │                  │    │
│  │ 全异步去中心化    │ │ 给rollout贴       │ │ 解耦裁剪防       │    │
│  │ 架构，千卡集群    │ │ "保鲜期"标签，    │ │ 熵崩塌，动态     │    │
│  │ 异步开销降到最低  │ │ 过期直接丢弃重采  │ │ 采样过滤噪声     │    │
│  │                  │ │                  │ │                  │    │
│  │ → P6 详细展开    │ │ → P7 详细展开    │ │ → P8 详细展开    │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "现在我们沿R3数据流逐层展开。第一站：Reply环节——回放更新，这是算法层的主战场。
>
> 这个环节承载两个子挑战：C1策略时效性和C4奖励与归因。先聚焦C1。
>
> 请看右上角这个对抗关系——吞吐量和新鲜度不可兼得。训推分离让推理集群全速跑 rollout，吞吐量上去了。但 on-policy RL 要求数据来自最新策略。异步意味着什么？rollout 生成时用的参数，等训练拿到时已经过期了。
>
> 在 Agent 场景这个问题被放大——一个 Episode 动辄几分钟，比 chatbot 长一到两个量级，陈旧度按分钟级累积。
>
> 怎么解？请看下面三个卡片，三个栈层各有应对。系统架构层，PRIME-RL 做千卡级全异步去中心化，从架构上把异步开销降到最低。训练框架层，SkyRL 给每条 rollout 贴保鲜期标签，过期直接丢弃重采。Off-Policy算法层，Decoupled PPO + V-trace 用重要性采样修正处理异步带来的策略偏移，让训练在off-policy数据上也能收敛。
>
> 三层从系统到框架到算法，层层递进。接下来三页逐一展开。"

---

## P6 | 技术①-1：PRIME-RL

### PPT内容

**标题**：PRIME-RL：千卡级全异步 RL，从架构层面降低异步代价
**标签**：Reply环节 · C1策略时效性 · 系统架构层 · PrimeIntellect（美国）

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：PRIME-RL：千卡级全异步 RL，从架构层面降低异步代价                  │
│  标签：Reply环节 · C1策略时效性 · 系统架构层 · PrimeIntellect（美国）   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】方案        │ │
│  │                                      │  │                     │ │
│  │  传统同步 RL 在千卡规模下，            │  │  ● 去中心化调度      │ │
│  │  最慢的 worker 决定整体速度            │  │    worker P2P协同   │ │
│  │  （木桶效应）                         │  │                     │ │
│  │                                      │  │  ● 1000+ GPU 集群   │ │
│  │  切到异步后，中心化调度器              │  │    原生multi-turn   │ │
│  │  成为新的瓶颈                         │  │    Agent交互        │ │
│  │                                      │  │                     │ │
│  │  ┌────────────────────────┐          │  │  ● FSDP2训练 +      │ │
│  │  │ 同步：木桶效应          │          │  │    vLLM推理 +       │ │
│  │  │ GPU1 ████████          │          │  │    FP8降内存        │ │
│  │  │ GPU2 ████████████████  │          │  │                     │ │
│  │  │ GPU3 ████████████      │          │  │  ● 已训出           │ │
│  │  │      ↑等最慢的          │          │  │    INTELLECT-3      │ │
│  │  └────────────────────────┘          │  │    (106B MoE)       │ │
│  │                                      │  │    多项同规模SOTA    │ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：系统架构本身要让"从生成到训练"的延迟足够短          │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU 集群的异步训练调度能力、跨节点通信效率              │    │
│  │  直接影响 Agentic RL 的数据新鲜度                             │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "PRIME-RL 来自 PrimeIntellect，解决的是架构层面的异步代价。
>
> 请看左侧问题——传统同步 RL 在千卡规模下有木桶效应，最慢的 worker 决定整体速度。切到异步后，中心化调度器又成为新瓶颈。
>
> 右侧是他们的方案：去中心化调度，worker 之间 P2P 协同，不依赖中心节点。支持 1000+ GPU 集群，原生支持 multi-turn Agent 交互。技术栈是 FSDP2 训练加 vLLM 推理加 FP8 降内存。已经训出了 INTELLECT-3——一个 106B 的 MoE 模型，多项同规模 SOTA。
>
> 底部关键逻辑：系统架构本身要让'从生成到训练'的延迟足够短。对我们的启示是——NPU 集群的异步训练调度能力和跨节点通信效率，直接影响 Agentic RL 的数据新鲜度。这是基础设施层面的竞争。"

---

## P7 | 技术①-2：SkyRL

### PPT内容

**标题**：SkyRL：给 Rollout 贴"保鲜期"，过期数据不进训练循环
**标签**：Reply环节 · C1策略时效性 · 训练框架层 · UC Berkeley

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：SkyRL：给 Rollout 贴"保鲜期"，过期数据不进训练循环              │
│  标签：Reply环节 · C1策略时效性 · 训练框架层 · UC Berkeley            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】方案        │ │
│  │                                      │  │                     │ │
│  │  异步架构下，推理集群生成的            │  │  ● 每条rollout携带  │ │
│  │  rollout 到达训练侧时，               │  │    策略版本号        │ │
│  │  策略可能已更新多轮。                  │  │                     │ │
│  │                                      │  │  ● 训练侧设置       │ │
│  │  用过期数据训练 → 梯度偏离            │  │    陈旧度阈值        │ │
│  │  → 严重时训练发散                     │  │                     │ │
│  │                                      │  │  ● 超阈值→丢弃      │ │
│  │  ┌────────────────────────┐          │  │    触发重采样        │ │
│  │  │ 示意：                  │          │  │                     │ │
│  │  │ Rollout(v3) → 训练(v7) │          │  │  ● 轻度陈旧用       │ │
│  │  │ 版本差=4，已过期!       │          │  │    importance       │ │
│  │  └────────────────────────┘          │  │    sampling补偿     │ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：不是所有异步生成的数据都值得训练，框架层要有质量门禁 │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：训练框架需支持策略版本追踪和条件过滤                     │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "PRIME-RL 从架构上降低延迟，但异步数据仍然可能过期。SkyRL 在框架层加了一道质量门禁。
>
> 请看左侧示意——Rollout 版本 3 到达训练侧时，策略已经更新到版本 7，版本差 4，这条数据已经过期了。用过期数据训练会导致梯度偏离，严重时训练发散。
>
> SkyRL 的方案很直接：每条 rollout 携带策略版本号，训练侧设置陈旧度阈值。超过阈值直接丢弃，触发重采样。轻度陈旧的数据用 importance sampling 做权重补偿。
>
> 关键逻辑是：不是所有异步生成的数据都值得训练，框架层必须有质量门禁。对我们的启示是——训练框架需要支持策略版本追踪和条件过滤，这是异步 RL 的基础能力。"

---

## P8 | 技术①-3：Off-Policy PPO

### PPT内容

**标题**：Off-Policy PPO：从算法层面处理异步带来的策略偏移
**标签**：Reply环节 · C1策略时效性 · 训练算法层 · Decoupled PPO + V-trace/TIS

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Off-Policy PPO：从算法层面处理异步带来的策略偏移                 │
│  标签：Reply环节 · C1策略时效性 · 训练算法层 · Decoupled PPO + V-trace/TIS │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】三大机制    │ │
│  │                                      │  │                     │ │
│  │  异步训练中actor和learner分离，        │  │ ┌─────────────────┐│ │
│  │  rollout数据由旧策略π_old生成，        │  │ │ Decoupled PPO   ││ │
│  │  learner用新策略π_new学习。            │  │ │ Actor-Learner   ││ │
│  │                                      │  │ │ 分离训练架构     ││ │
│  │  策略偏移（policy lag）导致：          │  │ │ Actor持续产生    ││ │
│  │  - importance ratio偏离1             │  │ │ rollout，Learner ││ │
│  │  - 梯度估计方差增大                   │  │ │ 异步消费并更新   ││ │
│  │  - 训练不稳定甚至发散                 │  │ └─────────────────┘│ │
│  │                                      │  │ ┌─────────────────┐│ │
│  │  ┌────────────────────────┐          │  │ │ V-trace          ││ │
│  │  │ 策略偏移示意：            │          │  │ │ 截断重要性采样   ││ │
│  │  │ π_old ─ rollout → data │          │  │ │ 用截断IS权重     ││ │
│  │  │            ↓ lag        │          │  │ │ c̄ = min(c, ρ_t) ││ │
│  │  │ π_new ─ learn → update │          │  │ │ 控制方差上界     ││ │
│  │  │ ratio = π_new/π_old    │          │  │ └─────────────────┘│ │
│  │  │ 偏离越大，梯度越不可靠   │          │  │ ┌─────────────────┐│ │
│  │  └────────────────────────┘          │  │ │ TIS权重修正      ││ │
│  │                                      │  │ │ 重要性权重修正   ││ │
│  │                                      │  │ │ 对off-policy数据 ││ │
│  │                                      │  │ │ 做importance     ││ │
│  │                                      │  │ │ weight加权       ││ │
│  │                                      │  │ │ 纠正分布偏差     ││ │
│  │                                      │  │ └─────────────────┘│ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：异步架构天然产生off-policy数据，算法必须能修正偏移    │    │
│  │  核心：用重要性采样（IS）纠正策略分布差异，V-trace截断控制方差  │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU的RL kernel需支持off-policy importance weight      │    │
│  │       计算、V-trace修正算子                                   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "架构降延迟、框架做门禁，但异步训练中策略偏移是根本问题。Off-Policy PPO从算法层面直接处理这个偏移。
>
> 请看左侧——异步架构下，Actor和Learner分离运行。Actor用旧策略生成rollout数据，Learner用新策略学习。两者之间存在策略版本差，importance ratio偏离1，梯度估计方差增大，训练不稳定甚至发散。
>
> 右侧三大机制。第一，Decoupled PPO：把Actor和Learner彻底分离，Actor持续产生rollout，Learner异步消费并更新——这是架构前提。第二，V-trace：用截断重要性采样来修正off-policy偏差，关键在于截断——把IS权重限制在一个上界内，控制方差不爆炸。第三，TIS重要性权重修正：对off-policy数据做importance weight加权，纠正新旧策略的分布差异。
>
> 关键逻辑是：异步架构天然产生off-policy数据，不能假装它是on-policy的——算法必须能显式修正偏移。对我们的启示——NPU的RL kernel需要支持off-policy importance weight计算和V-trace修正算子。
>
> Reply环节小结：C1策略时效性从三层应对——架构降延迟、框架做门禁、算法修偏移。接下来沿R3数据流进入Routing环节。"

---

## P9 | Routing环节：采集吞吐与推理效率（C2+C3）

### PPT内容

**标题**：Routing环节：采集吞吐与推理效率（C2+C3）— 推理集群的有状态转型

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Routing环节：采集吞吐与推理效率（C2+C3）                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区                          张力示意               │     │
│  │                                                            │     │
│  │  训推分离把环境交互全放到           ┌────────┐ ←→ ┌──────┐ │     │
│  │  推理侧。三重复杂度叠加：          │有状态   │     │高吞吐│ │     │
│  │                                   └────────┘     └──────┘ │     │
│  │  ① 环境并发管理（千级sandbox）                               │     │
│  │  ② KV cache 局部性 vs 调度灵活性                            │     │
│  │  ③ 工具调用长尾延迟拖拽整条rollout                           │     │
│  │                                                            │     │
│  │  单独看都能解，叠在一起不是线性增长的复杂度                    │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 推理调度层         │ │ 存储/缓存层       │ │ 硬件/内存层       │    │
│  │                  │ │                  │ │                  │    │
│  │ NVIDIA Dynamo    │ │ LMCache          │ │ Mooncake      │    │
│  │                  │ │                  │ │                  │    │
│  │ KV感知路由，      │ │ 跨引擎P2P共享    │ │ CXL共享内存替代  │    │
│  │ 请求去已有缓存   │ │ KV cache，打破   │ │ 网络传输KV，     │    │
│  │ 的节点           │ │ 节点孤岛         │ │ 消除硬件瓶颈     │    │
│  │                  │ │                  │ │                  │    │
│  │ → P10 详细展开   │ │ → P11 详细展开   │ │ → P12 详细展开   │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "R3数据流的第二站：Routing环节——路由请求，这是框架层的主战场。
>
> 这个环节承载两个子挑战：C2采集吞吐和C3推理效率。
>
> 请看右上角这个对抗关系——有状态和高吞吐不可兼得。训推分离把环境交互全放到推理侧，三重复杂度叠加。
>
> 第一，环境并发管理——千级 sandbox 同时运行，这是C2采集吞吐的核心痛点。第二，KV cache 局部性和调度灵活性的矛盾——Agent 多轮对话大量 prefix 可复用，但调度器不知道哪个节点有哪段 KV cache。第三，70B模型不量化单卡放不下，工具调用的长尾延迟拖拽整条 rollout——这是C3推理效率的核心痛点。
>
> 单独看都能解，叠在一起不是线性增长的复杂度。
>
> 下面三个卡片，三个栈层各有应对。推理调度层，NVIDIA Dynamo 做 KV 感知路由。存储缓存层，LMCache 做跨引擎 P2P 共享 KV cache。分离式架构层，Mooncake 用RDMA+分布式KV Store实现prefill-decode分离和KV缓存池化——这是月之暗面已在生产环境部署的方案。接下来三页逐一展开。"

---

## P10 | 技术②-1：NVIDIA Dynamo

### PPT内容

**标题**：NVIDIA Dynamo：KV 感知路由，让请求去已有缓存的节点
**标签**：Routing环节 · C2采集吞吐 · 推理调度层

**问题**：Agent 多轮对话中大量 prefix 可复用，但标准负载均衡不知道哪个节点有哪段 KV cache，导致重复计算。

**方案**：
- NVIDIA GTC 2025 发布，开源推理框架
- KV-aware Router：Radix Tree 追踪全集群 KV 分布，计算 cache overlap score
- NIXL：点对点 KV cache 传输库
- KV Block Manager：多层级内存管理（GPU VRAM → CPU RAM → SSD）
- Prefill-Decode 分离

**关键逻辑**：推理调度从"哪个节点空闲"变成"哪个节点已有我需要的 KV"

**启示**：NPU 推理引擎需要支持 KV cache 元数据暴露和跨节点迁移能力

### 演讲话术（60-90秒）

> "NVIDIA Dynamo，GTC 2025 发布的开源推理框架，解决推理调度层的问题。
>
> 核心问题：Agent 多轮对话大量 prefix 可复用，但标准负载均衡只看哪个节点空闲，不知道哪个节点有我需要的 KV cache。结果是同一段上下文被不同节点重复计算。
>
> Dynamo 的方案是 KV-aware Router——用 Radix Tree 追踪全集群 KV 分布，计算 cache overlap score，把请求路由到已有缓存的节点。配合 NIXL 点对点传输库和多层级内存管理，实现 Prefill-Decode 分离。
>
> 关键逻辑：推理调度从'哪个节点空闲'变成'哪个节点已有我需要的 KV'。对我们的启示——NPU 推理引擎需要支持 KV cache 元数据暴露和跨节点迁移能力，这是 Agent 推理场景的刚需。"

---

## P11 | 技术②-2：LMCache

### PPT内容

**标题**：LMCache：跨引擎 P2P 共享 KV cache，打破节点孤岛
**标签**：Routing环节 · C2+C3 · 存储缓存层

**问题**：每个推理引擎独立维护 KV cache，同一 prefix 被多个引擎重复计算。Agent 场景 95%+ context 可复用。

**方案**：
- 开源项目，集成 vLLM、SGLang、KServe
- P2P 共享：引擎间直接传输 KV cache
- Cache Offloading：GPU VRAM → CPU RAM 多级卸载
- Connector 接口：标准化 KV cache 存取 API
- 实测：多轮 QA 15x 吞吐提升，TTFT 降低 54.7%

**关键逻辑**：KV cache 不应是引擎私有资源，而应是集群级的共享池

**启示**：NPU 推理软件栈需支持标准化 KV cache 存取接口和高效跨节点传输

### 演讲话术（60-90秒）

> "Dynamo 解决路由——把请求送到正确的节点。LMCache 解决的是更底层的问题——KV cache 本身怎么共享。
>
> 问题很直观：每个推理引擎独立维护自己的 KV cache，同一段 prefix 被多个引擎重复计算。Agent 场景下 95% 以上的 context 可以复用，重复计算的浪费是巨大的。
>
> LMCache 是开源项目，集成了 vLLM、SGLang、KServe。核心是 P2P 共享——引擎间直接传输 KV cache，加上 GPU VRAM 到 CPU RAM 的多级卸载，和标准化的 KV cache 存取 API。
>
> 实测结果：多轮 QA 场景 15 倍吞吐提升，首 token 延迟降低 54.7%。
>
> 关键逻辑是：KV cache 不应是引擎私有资源，而应是集群级的共享池。对我们的启示——NPU 推理软件栈需要支持标准化 KV cache 存取接口和高效跨节点传输。"

---

## P12 | 技术②-3：Mooncake

### PPT内容

**标题**：Mooncake：KV Cache分离架构的生产级实践
**标签**：Routing环节 · C2+C3 · 推理架构层 · 月之暗面（Moonshot AI）

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Mooncake：KV Cache分离架构的生产级实践                          │
│  标签：Routing环节 · C2+C3 · 推理架构层 · 月之暗面（Moonshot AI）     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】三大机制    │ │
│  │                                      │  │                     │ │
│  │  Agent长程对话场景下，KV Cache:        │  │ ┌─────────────────┐│ │
│  │  - 单会话可达数GB                     │  │ │ KVCache-centric  ││ │
│  │  - 千并发撑爆GPU显存                  │  │ │ Disaggregated    ││ │
│  │  - Prefill和Decode对资源需求          │  │ │ Architecture     ││ │
│  │    截然不同（计算密集 vs 访存密集）     │  │ │                  ││ │
│  │                                      │  │ │ 以KV Cache为中心  ││ │
│  │  传统方案把KV Cache绑定在             │  │ │ 做prefill-decode  ││ │
│  │  生成它的GPU上——                     │  │ │ 分离调度          ││ │
│  │  扩展性受单机显存硬约束               │  │ └─────────────────┘│ │
│  │                                      │  │ ┌─────────────────┐│ │
│  │  ┌────────────────────────┐          │  │ │ RDMA + 分布式    ││ │
│  │  │ 瓶颈示意：              │          │  │ │ KV Store         ││ │
│  │  │ GPU_A: [KV][KV][KV]满 │          │  │ │                  ││ │
│  │  │ GPU_B: [  ][  ][  ]空 │          │  │ │ KV Cache通过RDMA ││ │
│  │  │        ↑ 无法迁移       │          │  │ │ 跨节点传输，存入  ││ │
│  │  └────────────────────────┘          │  │ │ 分布式KV Store   ││ │
│  │                                      │  │ │ 解耦存储与计算   ││ │
│  │                                      │  │ └─────────────────┘│ │
│  │                                      │  │ ┌─────────────────┐│ │
│  │                                      │  │ │ Prefill-Decode   ││ │
│  │                                      │  │ │ 分离             ││ │
│  │                                      │  │ │                  ││ │
│  │                                      │  │ │ Prefill节点专做  ││ │
│  │                                      │  │ │ 计算密集的首次   ││ │
│  │                                      │  │ │ 编码，Decode节点 ││ │
│  │                                      │  │ │ 专做访存密集的   ││ │
│  │                                      │  │ │ 逐token生成      ││ │
│  │                                      │  │ └─────────────────┘│ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：月之暗面Kimi的生产系统验证了KV Cache分离架构         │    │
│  │  的可行性——不是学术概念，是真实线上流量验证过的方案              │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU推理架构需支持KV Cache分离和跨节点池化，              │    │
│  │       HCCS是天然优势                                         │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**问题**：Agent长程对话场景下，单会话KV Cache可达数GB，千并发撑爆GPU显存。传统方案KV Cache绑定在生成它的GPU上，无法跨节点迁移，扩展性受单机显存硬约束。

**方案**：
- 月之暗面（Moonshot AI）Kimi生产系统的推理架构
- KVCache-centric Disaggregated Architecture：以KV Cache为中心重新设计推理调度
- RDMA + 分布式KV Store：KV Cache通过RDMA跨节点传输，存入分布式KV Store，解耦存储与计算
- Prefill-Decode分离：Prefill节点专做计算密集的首次编码，Decode节点专做访存密集的逐token生成
- 已在Kimi线上生产环境验证，不是学术概念

**关键逻辑**：KV Cache分离架构是解决有状态推理扩展性的生产级方案，已经过真实流量验证

**启示**：NPU推理架构需支持KV Cache分离和跨节点池化，HCCS是天然优势

### 演讲话术（60-90秒）

> "Dynamo做路由，LMCache做共享，但更根本的问题是——KV Cache绑定在单GPU上，扩展性受单机显存硬约束。Mooncake从架构层彻底解决。
>
> 这是月之暗面Kimi的生产系统架构，不是学术论文，是真实线上流量验证过的方案。核心思路：以KV Cache为中心做disaggregated architecture——把KV Cache从GPU上解耦出来，通过RDMA跨节点传输，存入分布式KV Store。
>
> 三个关键设计：第一，KVCache-centric的分离架构，KV Cache不再绑定在生成它的GPU上，可以自由迁移和池化。第二，RDMA加分布式KV Store，高带宽低延迟的跨节点KV传输。第三，Prefill-Decode分离——Prefill是计算密集的，Decode是访存密集的，放在一起互相干扰，分开后各自优化。
>
> 关键逻辑：这不是一篇学术论文提出的概念，而是月之暗面在Kimi的生产环境中实际运行的架构，已经被真实流量验证。这对我们的启示——NPU推理架构需要支持KV Cache分离和跨节点池化，而HCCS互联恰好是这方面的天然优势。
>
> Routing环节小结：C2采集吞吐和C3推理效率从三层应对——调度做KV感知路由，存储做跨引擎共享，架构做KV Cache分离池化。接下来沿R3数据流进入Record环节。"

---

## P13 | Record环节：环境保真与工程可达（C6+C7）

### PPT内容

**标题**：Record环节：环境保真与工程可达（C6+C7）— 大规模探索的基础设施挑战

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Record环节：环境保真与工程可达（C6+C7）                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区（详细版）               张力示意                 │     │
│  │                                                            │     │
│  │  大规模并行 rollout 是 Agentic    ┌────────┐ ←→ ┌──────┐  │     │
│  │  RL 的核心优势——千路并行探索，     │探索效率 │     │安全性│  │     │
│  │  数据生成速度远超人类标注。        └────────┘     └──────┘  │     │
│  │                                                            │     │
│  │  但并行度越高，风险越大：                                    │     │
│  │                                                            │     │
│  │  ① 归因困难：Agent 一条轨迹几十步甚至上百步，                │     │
│  │     哪一步是关键决策？trajectory 级奖励太粗，                │     │
│  │     无法区分"碰巧做对"和"真正学会"                          │     │
│  │                                                            │     │
│  │  ② 过程失控：Agent 在探索中可能陷入死循环、                  │     │
│  │     冗余操作、永不终止——消耗算力却无有效信号                  │     │
│  │                                                            │     │
│  │  ③ 奖励黑客：开放任务的奖励定义模糊，                        │     │
│  │     并行探索越多越容易发现 reward hack 捷径，                │     │
│  │     模型学到"作弊"而非"做事"                                │     │
│  │                                                            │     │
│  │  三个问题叠加 = 系统性风险，不是调参能解的                    │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 归因算法层         │ │ 奖励建模层        │ │ 探索策略层        │    │
│  │                  │ │                  │ │                  │    │
│  │ SALT (Amazon)    │ │ SWE-PRM (学术)   │ │ HiPER (学术)     │    │
│  │                  │ │                  │ │                  │    │
│  │ 轨迹图推算       │ │ 轻量PRM实时检测  │ │ planner+executor │    │
│  │ step-level       │ │ 纠正轨迹问题     │ │ 分层优势估计     │    │
│  │ advantage        │ │ （冗余/死循环）   │ │                  │    │
│  │                  │ │                  │ │                  │    │
│  │ → P14 详细对比   │ │ → P14 详细对比   │ │ → P14 详细对比   │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "R3数据流的第三站：Record环节——记录轨迹，这是基础设施层的主战场。
>
> 这个环节承载两个子挑战：C6环境保真和C7工程可达。但在展开之前，我要先说一个贯穿全部的问题：大规模并行探索的奖励失控。
>
> 请看右上角——探索效率和安全性的对抗。大规模并行 rollout 是 Agentic RL 的核心优势——千路并行探索，数据生成速度远超人类标注。但并行度越高，风险越大。三个问题叠加。
>
> 第一，归因困难——这正是C4奖励与归因在Reply层的问题向Record层传导。Agent 一条轨迹几十步甚至上百步，只有末尾给一个成功或失败信号。到底哪一步是关键决策？
>
> 第二，过程失控——这是C6环境保真的直接体现。sandbox偏差导致Agent在探索中可能陷入死循环、冗余操作、永不终止——消耗算力却没有有效信号。
>
> 第三，工程可达性鸿沟——C7的痛点。算法2到4周迭代，但算子适配要3到6个月，开放任务的奖励定义模糊，并行探索越多越容易发现 reward hack 捷径。
>
> 三个问题叠加是系统性风险，不是调参能解的。下面三个卡片对应三个层面的解法——SALT 做归因、SWE-PRM 做过程监控、HiPER 做分层探索。下一页详细对比。"

---

## P14 | Record环节三技术详细对比

### PPT内容

**标题**：奖励失控的三层解法：归因 × 建模 × 策略

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：奖励失控的三层解法：归因 × 建模 × 策略                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  三技术详细对比（表格式，占页面主体）                           │    │
│  │                                                             │    │
│  │  维度       │ SALT (Amazon)    │ SWE-PRM (学术)  │ HiPER (学术)│
│  │  ───────────┼─────────────────┼────────────────┼────────────│
│  │  解决什么   │ 归因太粗          │ 推理过程失控    │ 探索太平    │
│  │             │ trajectory级     │ 冗余/死循环    │ flat policy│
│  │             │ 无法区分好坏     │ 永不终止       │ 方差大     │
│  │  ───────────┼─────────────────┼────────────────┼────────────│
│  │  核心思路   │ 不止看最终结果    │ 不等失败再纠正  │ 不在一层   │
│  │             │ 追踪每一步贡献   │ 过程中实时检测  │ 做所有决策 │
│  │  ───────────┼─────────────────┼────────────────┼────────────│
│  │  核心机制   │ 把Agent轨迹建模  │ 训练轻量PRM    │ 高层planner│
│  │             │ 为有向图，推算   │ 在rollout中    │ 分解子目标 │
│  │             │ step-level       │ 实时检测冗余/  │ 低层executor│
│  │             │ advantage        │ 循环/未终止    │ 执行+估计  │
│  │             │                 │ 并做纠正       │ 优势       │
│  │  ───────────┼─────────────────┼────────────────┼────────────│
│  │  验证效果   │ WebShop 等      │ SWE-bench      │ ALFWorld   │
│  │             │ 多项基准提升    │ +10.6pp        │ 97.4%      │
│  │  ───────────┼─────────────────┼────────────────┼────────────│
│  │  栈层       │ 归因算法层       │ 奖励建模层      │ 探索策略层  │
│  │  ───────────┼─────────────────┼────────────────┼────────────│
│  │  NPU启示    │ 图计算/推算能力  │ PRM并行推理    │ 双层模型   │
│  │             │ step-level      │ 低延迟         │ 编排调度   │
│  │             │ 信用分配        │                │            │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑（高亮条）：                                         │    │
│  │  奖励失控是三层问题：归因不准 × 过程不可控 × 探索不高效          │    │
│  │  三个技术分别在归因/建模/策略层应对，缺一层都不行               │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU 需同时支持图推算（归因）、PRM 并行推理（建模）、     │    │
│  │  双层模型编排（策略）—— 全栈能力决定奖励质量                   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "这张表把三个解法摊开对比。
>
> SALT 来自 Amazon，解决归因太粗的问题。把 Agent 轨迹建模为有向图，推算 step-level advantage——不止看最终结果，追踪每一步的贡献。在 WebShop 等多项基准上验证有效。
>
> SWE-PRM 解决过程失控。训练一个轻量级 PRM，在 rollout 过程中实时检测冗余、死循环、未终止，并做纠正。在 SWE-bench 上提升 10.6 个百分点。
>
> HiPER 解决探索效率。用高层 planner 分解子目标，低层 executor 执行并估计优势——不在一层做所有决策。在 ALFWorld 达到 97.4%。
>
> 请看底部关键逻辑：Record环节的问题是三层叠加——归因不准（C4传导）、过程不可控（C6环境保真）、工程鸿沟（C7工程可达）。三个技术分别在归因、建模、策略层应对，缺一层都不行。对我们的启示——NPU 需要同时支持图推算、PRM 并行推理、双层模型编排。全栈能力决定奖励质量。
>
> R3数据流三个环节——Reply、Routing、Record——六个子挑战全部展开完毕。最后一页收束。"

---

## P15 | 收束层：全栈协同 — 对我们团队的启示

### PPT内容

**标题**：全栈协同：三层架构调整 x R3数据流 x 6挑战全覆盖

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：全栈协同：三层架构调整 × R3数据流 × 6挑战全覆盖                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  核心判断（大字，居中，高亮背景）：                              │    │
│  │  6子挑战+横切约束沿R3数据流穿透三层架构                         │    │
│  │  三层架构调整 × 全栈协同 = 我们"帮客户开箱即用"的价值锚点       │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 🔵行动方向 ①      │ │ 🟢行动方向 ②      │ │ 🟠行动方向 ③      │    │
│  │ Reply：           │ │ Routing：         │ │ Record：          │    │
│  │ 训推协同基础设施   │ │ 有状态推理引擎     │ │ 可控探索平台       │    │
│  │                  │ │                  │ │                  │    │
│  │ 对应挑战：        │ │ 对应挑战：        │ │ 对应挑战：        │    │
│  │ C1 策略时效性     │ │ C2 采集吞吐       │ │ C6 环境保真       │    │
│  │ C4 奖励与归因     │ │ C3 推理效率       │ │ C7 工程可达       │    │
│  │                  │ │                  │ │                  │    │
│  │ 关键能力：        │ │ 关键能力：        │ │ 关键能力：        │    │
│  │ · 异步训练调度    │ │ · KV cache 共享  │ │ · 高并发rollout  │    │
│  │ · 跨节点通信优化  │ │   /迁移能力      │ │ · PRM 并行推理   │    │
│  │ · 策略版本管理    │ │ · 互联架构优化    │ │ · 分层策略编排   │    │
│  │ · step-level归因 │ │   (HCCS)        │ │ · sandbox保真    │    │
│  │                  │ │ · 多级内存管理    │ │                  │    │
│  │ 技术参考：        │ │                  │ │ 技术参考：        │    │
│  │ PRIME-RL / SkyRL │ │ 技术参考：        │ │ SALT / SWE-PRM  │    │
│  │ Off-Policy PPO   │ │ Dynamo / LMCache │ │ HiPER            │    │
│  │                  │ │ Mooncake         │ │                  │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  ⚪横切约束 C5（数值可信）── 穿透三个行动方向的共同底线         │    │
│  │  NPU精度+训推一致性 = 全栈协同的基础                            │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  收束（大字，高亮条）：                                        │    │
│  │  Agent 时代的竞争，不在谁的模型更大，而在谁的全栈更顺。         │    │
│  │  我们的机会：让 NPU 成为客户做 Agentic RL 后训练的最优选择。   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "最后收束。六个子挑战加横切约束，沿R3数据流穿透三层架构，需要全栈协同。
>
> 请看三个行动方向，按R3数据流排列，每个方向覆盖两个子挑战。
>
> 行动方向一：Reply环节——训推协同基础设施，对应C1策略时效性和C4奖励与归因。关键能力是异步训练调度、跨节点通信优化、策略版本管理，以及step-level归因。参考 PRIME-RL、SkyRL、Off-Policy PPO。
>
> 行动方向二：Routing环节——有状态推理引擎，对应C2采集吞吐和C3推理效率。关键能力是 KV cache 共享和迁移、HCCS 互联架构优化、多级内存管理。参考 Dynamo、LMCache、Mooncake。
>
> 行动方向三：Record环节——可控探索平台，对应C6环境保真和C7工程可达。关键能力是高并发 rollout、PRM 并行推理、分层策略编排、sandbox保真。参考 SALT、SWE-PRM、HiPER。
>
> 请注意底部这行——横切约束C5数值可信，穿透三个行动方向的共同底线。NPU上的算子精度和训推一致性是全栈协同的基础。如果底层数值不可信，上层所有优化都建立在沙滩上。这也是我们作为硬件平台方独有的优势。
>
> 910C 和 H100 的裸性能差距是 60%，但如果我们在精度和一致性上做到业界最好——确定性计算、批不变性、训推一致性——客户会因为训练结果可复现而选择我们，而不仅仅因为 FLOPS 便宜。
>
> 底线判断：**Agent 时代的竞争，不在谁的模型更大，而在谁的全栈更顺。三层架构调整沿R3数据流覆盖全部六个子挑战加横切，这就是我们的全景作战图。我们的机会是让 NPU 成为客户做 Agentic RL 后训练的最优选择——不是 GPU 的替代品，而是全栈优化的差异化平台。**
>
> 今天汇报的核心诉求：Agent RL 后训练是确定性的算力需求方向，我们的技术布局已经对准了R3数据流上的六大挑战，需要资源支持加速落地。谢谢各位领导。"

---

## 附：页码总览与逻辑流

```
P1  Agent时代已来           ← 定调：产品已落地+开源引爆+巨头真金白银
P2  演进+不可替代性（合并）  ← 动因：三阶段演进+排除法+桥接层+飞轮+巨头验证
P3  七维→R3×三层→6挑战+横切 ← 推导：七维对比→R3分组→6子挑战+C5横切
P4  全栈需求传导             ← 导航：R3×6挑战传导表→9技术
P5  Reply环节（C1+C4）      ← 策略时效性与奖励归因
P6  PRIME-RL                ← 系统架构层
P7  SkyRL                   ← 训练框架层
P8  Off-Policy PPO          ← Off-Policy算法层
P9  Routing环节（C2+C3）    ← 采集吞吐与推理效率
P10 Dynamo                  ← 推理调度层
P11 LMCache                 ← 存储缓存层
P12 Mooncake                ← 分离式架构层
P13 Record环节（C6+C7）     ← 环境保真与工程可达
P14 Record三技术详细对比     ← 归因×建模×策略（解法对比）
P15 全栈协同·团队启示        ← 收束：三层架构调整×R3数据流×6挑战全覆盖
```

**页间逻辑链**：现象(P1) → 论证why(P2) → 推导how(P3:6挑战+横切) → 导航map(P4) → 按R3逐层展开(P5-P14) → 收束行动(P15)

**R3数据流与挑战映射**：
- Reply（算法层）：C1策略时效性 + C4奖励与归因 → P5-P8
- Routing（框架层）：C2采集吞吐 + C3推理效率 → P9-P12
- Record（基础设施层）：C6环境保真 + C7工程可达 → P13-P14
- 横切C5（数值可信）：穿透R3全流程

**数据校准说明**：
- GitHub 星数：OpenClaw 24.7万（原始PPT数据）
- Anthropic RL 环境投入：表述为"讨论年投$1B+"（The Information 报道，非官方确认）
- 后训练算力 40%+：指广义后训练（含 RLHF+蒸馏+合成数据等）
- SFT 天花板：已修正为"训练分布≠部署分布 (distribution shift)"（P2P3-final.md 三方共识）
- 对比维度：六维→七维（新增"上下文"维度，归入组B）
- 挑战体系：3粗挑战→6子挑战+1横切（C1-C7），按R3数据流分组
- 六子挑战编号：C1策略时效性、C2采集吞吐、C3推理效率、C4奖励与归因、C5数值可信（横切）、C6环境保真、C7工程可达
