# PPT + 演讲稿 最终版

> 主题：Agent 时代下，Agentic RL 后训练的核心挑战与全栈应对
> 受众：洞察分享会（华为计算部门高级领导）
> 风格：华为/科技企业风格，深色背景
> 总计：15页，每页配60-90秒演讲话术

---

## P1 | 定调层：Agent 时代已来

### PPT内容

**标题**：智能涌现：Agent 从概念验证走向规模落地，AI 产业进入"做事"时代

**核心信息**：产品、开源、资本三个维度同时爆发——Agent 时代不是预测，是正在发生的现实。

```
┌─────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                     │
│  标题：智能涌现：Agent 从概念验证走向规模落地                            │
│  副标题：AI 产业进入"做事"时代                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────┐        │
│  │            顶部：一句话定调（大字，加粗，居中）              │        │
│  │  "从'能聊'到'能做'的分水岭已经跨过，Agent 是载体"           │        │
│  └─────────────────────────────────────────────────────────┘        │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 产品已落地         │ │ 开源正在引爆      │ │ 巨头真金白银      │    │
│  │  ——加速时间线      │ │                  │ │                  │    │
│  │                  │ │ ● Claude Code    │ │ ● Anthropic      │    │
│  │ 2024.3 Devin     │ │   代码泄露事件    │ │   讨论年投$1B+   │    │
│  │  编程Agent,端到端 │ │   (2026.3 npm    │ │   采购RL环境     │    │
│  │  开发             │ │    sourcemap     │ │   (The Info.)    │    │
│  │                  │ │    意外暴露51万行 │ │                  │    │
│  │ 2025.1 Operator  │ │    源码)         │ │ ● OpenAI         │    │
│  │  GUI Agent       │ │                  │ │   RFT 对o4-mini  │    │
│  │  (后升级为       │ │ ● OpenClaw       │ │   公开发布       │    │
│  │   Agent Mode)    │ │   基于泄露代码    │ │   (2025.5)       │    │
│  │                  │ │   社区重写的通用  │ │                  │    │
│  │ 2025.3 Manus     │ │   Agent框架      │ │ ● Google         │    │
│  │  通用Agent,多步骤│ │   一天10万星     │ │   Gemini 2.5整合 │    │
│  │  自主完成        │ │   24.7万GitHub星 │ │   Agent RL训练   │    │
│  │                  │ │   集成20+平台    │ │   多步骤+工具    │    │
│  │ 2025.7           │ │                  │ │                  │    │
│  │  ChatGPT Agent   │ │ ● 开源小模型     │ │ ● Microsoft      │    │
│  │  Mode（替代      │ │   在Agent场景    │ │   Copilot→Agent  │    │
│  │  Operator）      │ │   追平大模型     │ │   编排平台       │    │
│  │                  │ │                  │ │                  │    │
│  │ 2025.10          │ │                  │ │                  │    │
│  │  Claude Computer │ │                  │ │                  │    │
│  │  Use GA          │ │                  │ │                  │    │
│  │                  │ │                  │ │                  │    │
│  │  ↓ 节奏在加快     │ │                  │ │                  │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│          ↓                    ↓                    ↓                │
│  ┌─────────────────────────────────────────────────────────┐        │
│  │ 底部汇聚条：产品从早期探索走向成熟，开源社区用脚投票，       │        │
│  │ 巨头用真金白银下注 → Agent 时代已来                        │        │
│  └─────────────────────────────────────────────────────────┘        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**内容要素**：

- **左列：产品已落地**（时间线呈现加速节奏）
  - 2024.3 Devin：编程 Agent，端到端完成开发任务
  - 2025.1 Operator → 2025.7 ChatGPT Agent Mode
  - 2025.3 Manus：通用 Agent，多步骤任务自主完成
  - 2025.10 Claude Computer Use GA：桌面级 Agent 正式版
- **中列：开源正在引爆**
  - Claude Code 代码泄露：2026.3 npm sourcemap 意外暴露 51.2 万行源码
  - OpenClaw：社区基于泄露代码重写的通用 Agent 框架，24.7 万 GitHub 星
  - 开源小模型在 Agent 场景追平甚至超越大模型
- **右列：巨头真金白银**
  - Anthropic：讨论年投 $1B+ 采购 RL 环境（The Information 报道）
  - OpenAI：RFT 2025.5 对 o4-mini 公开发布
  - Google：Gemini 2.5 训练流程中 RL 阶段支持多步骤动作+工具调用
  - Microsoft：Copilot 从"副驾驶"升级为 Agent 编排平台

### 演讲话术（60-90秒）

> "各位领导，今天不讲趋势综述，直接说结论：Agent 已经从概念验证进入规模落地，产品、开源、资本三条线在 2026 年 Q1 同时爆发。
>
> 先看产品端。请看左列时间线——从 2024 年 Devin 做概念验证，到 2025 年 ChatGPT Agent Mode 替代 Operator，再到 Claude Computer Use 正式版发布，节奏在明显加快。特别是 Cursor Composer 2，一家 IDE 公司用 RL 训练自研模型打败了 Claude Opus 4.6——这说明 Agent RL 已经是产品竞争力的核心。
>
> 再看开源端。中间这列——Claude Code 源码泄露后，社区基于它重写了 OpenClaw 框架，24.7 万 GitHub 星。DeepSWE 用 RL 训练开源 32B 模型达到 Pass@1 42.2%。开源做 Agent 的门槛已经降到可触达。
>
> 最后看资本端。右列——据 The Information 报道，Anthropic 领导层讨论年投超 10 亿美元采购 RL 训练环境。广义后训练算力占总算力份额持续攀升到约 40%。三家路线不同，但共识一致。
>
> 这三条线同时爆发意味着：Agent 后训练不再是实验室项目，而是真实的算力负载——我们的 NPU 平台必须能承接这类负载。这是今天汇报的出发点。"

---

## P2 | 动因层：后训练演进与 Agentic RL 的不可替代性

### PPT内容

**标题**：不可替代：从 SFT 到 Agentic RL，Agent 能力提升只有一条路

```
┌──────────────────────────────────────────────────┐
│ [深色渐变头部条]                                    │
│ 标题：不可替代：从 SFT 到 Agentic RL，             │
│       Agent 能力提升只有一条路                      │
├──────────────────────────────────────────────────┤
│                                                  │
│ 【上半页：三阶段演进横向箭头 + 排除法】【原有+微调】  │
│                                                  │
│ ┌──────────────┐  →  ┌──────────────┐  →  ┌──────────────┐
│ │ 第一阶段       │     │ 第二阶段       │     │ 第三阶段 ★    │
│ │ SFT / RLHF    │     │ DPO / KTO     │     │ Agentic RL   │
│ │               │     │               │     │              │
│ │ 模型：GPT-4   │     │ 模型：Llama3.3│     │ 模型：        │
│ │ Llama-2-Chat  │     │ Qwen-2.5     │     │ GPT-5.4      │
│ │ ChatGLM-3     │     │ Mistral Lg.2 │     │ Claude Op4.6 │
│ │               │     │               │     │ Gemini 3.1Pro│
│ │ 算力：<5%     │     │ 算力：5-15%   │     │ DeepSeek-V3.2│
│ │ 场景：3-5种   │     │ 场景：5-10种  │     │ Llama4 Mav.  │
│ │ (对话/问答)   │     │ (+偏好/安全)  │     │ GLM-5        │
│ │               │     │               │     │              │
│ │ ┌──────────┐ │     │ ┌──────────┐ │     │ 算力：40%+   │
│ │ │✗ 天花板   │ │     │ │✗ 天花板   │ │     │ 场景：50+种  │
│ │ │状态空间大 │ │     │ │奖励来自  │ │     │              │
│ │ │演示覆盖  │ │     │ │环境不来  │ │     │ ┌──────────┐ │
│ │ │不了；    │ │     │ │自偏好；  │ │     │ │✓ 唯一路径 │ │
│ │ │训练分布  │ │     │ │能教对齐  │ │     │ │环境反馈   │ │
│ │ │≠部署分布 │ │     │ │不能教交互│ │     │ │覆盖无限态 │ │
│ │ │(dist.    │ │     │ └──────────┘ │     │ │从错误中学 │ │
│ │ │ shift)   │ │     │              │     │ └──────────┘ │
│ │ └──────────┘ │     └──────────────┘     │              │
│ └──────────────┘                          └──────────────┘
│                                                  │
│ 中间一句话（高亮）：                                │
│ 后训练算力<5%→40%+，Agent必须在做事中学会做事       │
│                                                  │
│ ┌────────────────────────────────────────────────┐│
│ │【桥接层——为什么RL是"唯一闭环方法"】               ││
│ │                                                ││
│ │ 金句（高亮）：                                   ││
│ │ "Harness的能力图，就是未来模型后训练的负载"       ││
│ │                                                ││
│ │ 公式：Agent能力 = 模型 + Harness               ││
│ │ 方向：Harness今天做的 → 模型明天要内化的         ││
│ │ 唯一性：SFT/DPO教静态映射；                      ││
│ │         RL = 唯一能在环境闭环中吸收能力的方法     ││
│ │                                                ││
│ │ 迁移实证（仅保留一行）：                          ││
│ │ ┌──────────────────────────────────────────┐   ││
│ │ │ 上下文压缩：Harness侧规则 → 模型内生能力  │   ││
│ │ │ Cursor Composer 2 用RL训self-summarization│   ││
│ │ │ 压缩错误↓50%，5000+token → ~1000token     │   ││
│ │ └──────────────────────────────────────────┘   ││
│ └────────────────────────────────────────────────┘│
│                                                  │
│ 【下半页：飞轮 + 巨头验证】                         │
│                                                  │
│    ┌──────────┐                                  │
│    │ 模型更强  │←────────────────┐                │
│    └────┬─────┘                 │                │
│         ↓                       │                │
│  ┌────────────┐          ┌──────┴─────┐          │
│  │在环境中做事 │          │更新模型参数  │          │
│  │(Harness)   │          │            │          │
│  └──────┬─────┘          └──────┬─────┘          │
│         │                       ↑                │
│   Anthropic                Google                │
│   $1B+采购环境             Gemini全流程RL         │
│         │                       ↑                │
│         ↓                       │                │
│  ┌────────────┐          ┌──────┴─────┐          │
│  │产生交互轨迹 │─────────→│得到环境反馈  │          │
│  └────────────┘          └────────────┘          │
│                                ↑                  │
│                           OpenAI                  │
│                           RFT开放grader           │
│                                                  │
│  三家全部押注RL后训练，没有一家认为SFT够用          │
│                                                  │
│ ┌──────────────────────────────────────────────┐ │
│ │ 结论（高亮）：                                  │ │
│ │ Agentic RL后训练 = Agent能力提升的不可替代路径  │ │
│ │ → 但直接把LLM RL搬到Agent场景，会结构性失配    │ │
│ │ → 接下来看：七维差异沿R3数据流催生哪六大挑战？  │ │
│ └──────────────────────────────────────────────┘ │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "P1 我们看到 Agent 时代已经来了。那凭什么说 Agentic RL 后训练是关键？这一页用排除法回答。
>
> 请看上半页三个阶段。第一代 SFT/RLHF，天花板是训练分布不等于部署分布——Agent 的状态空间太大，演示覆盖不了。第二代 DPO/KTO，天花板是奖励来自环境验证而非人类偏好——能教对齐不能教交互。排除完前两条路，只剩 Agentic RL：环境反馈覆盖无限状态，从错误中学习。
>
> 排除法告诉我们只剩 RL 一条路，但**为什么 RL 就能行**？
>
> 一个关键洞察：Agent 的能力等于模型加上 Harness——就是围绕模型的工程脚手架。今天 Harness 做的上下文压缩、工具编排、错误恢复，明天都要被模型内化。换句话说，**Harness 的能力图，就是未来模型后训练的负载**。
>
> SFT 只能教静态输入输出映射，DPO 只能教偏好排序，它们都无法让模型在做事中学会做事。RL 是唯一能通过环境闭环让模型吸收这些动态能力的方法。
>
> 一个已经落地的实证：Cursor Composer 2 用 RL 训练 self-summarization，把上下文压缩从 Harness 侧的工程方案变成模型的内生能力，压缩错误降了 50%，摘要长度从 5000 多 token 压到 1000 左右。
>
> 再看下半页飞轮——Anthropic 投环境、OpenAI 开放 grader、Google 做全流程 RL。三家全部押注 RL 后训练，没有一家认为 SFT 够用。
>
> 所以结论很清楚：Agentic RL 后训练是 Agent 能力提升的不可替代路径。但——直接把传统 LLM RL 搬到 Agent 场景，会结构性失配。接下来我们看，到底哪里不同。"

---

## P3 | 七维变化 → 五挑战+横切（直接展示）

### PPT内容

**标题**：范式跃迁：七维结构性变化催生五大挑战+横切约束

**核心信息**：左半页用七维对比表展示LLM RL与Agentic RL的差异，右半页直接按五个挑战展示（不按层分组），每个挑战一行：名称+来源维度+量化严重性。底部过渡到P4三层架构升级。挑战本身不预设分层——分层是P4解法的组织方式。

**P2→P3 承接**：P2 论证了"必须走 Agentic RL"，P3 回答"Agentic RL 和 LLM RL 到底有什么不同？这些差异直接造成哪五大挑战？"

```
┌──────────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                          │
│  标题：范式跃迁：七维变化 → 五大挑战+横切                                  │
├────────────────────────────┬─────────────────────────────────────────────┤
│                            │                                             │
│  【左半页 40%】             │  【右半页 60%】                               │
│  LLM RL vs Agentic RL     │  五大挑战——直接从维度正推                     │
│  七维对比表                 │  每行：名称 | 来源维度 | 量化严重性            │
│                            │                                             │
│  ┌──────────────────────┐  │  ┌──────────────────────────────────────┐   │
│  │ 维度   │LLM RL│Agntc RL│  │  │                                      │   │
│  │ ───────┼──────┼────────│  │  │  ❶ 长序列推理开销                      │   │
│  │ ①算力  │训练主│推理90% │  │  │     维度①+⑦ │ 单Agent会话KV数GB      │   │
│  │ ②环境  │无    │真实    │  │  │     千并发撑爆显存，70B不量化放不下     │   │
│  │ ③数据  │标注  │在线轨迹│  │  │     ★★★★★（最高频痛点）               │   │
│  │ ④吞吐  │离线  │在线流式│  │  │                                      │   │
│  │ ⑤奖励  │人评  │环境验证│  │  │  ❷ 环境交互延迟                        │   │
│  │ ⑥探索  │KL约束│大探索  │  │  │     维度②+③ │ Episode分钟级          │   │
│  │ ⑦上下文│短    │M级长程 │  │  │     工具调用长尾拖拽rollout            │   │
│  │                        │  │  │     ★★★★☆                             │   │
│  │  七维差异 ────────→     │  │  │                                      │   │
│  │  直接正推出五个挑战     │  │  │  ❸ 在线数据吞吐瓶颈                    │   │
│  └──────────────────────┘  │  │     维度④ │ 异步数据天然过时            │   │
│                            │  │     版本差>4步发散（SkyRL）              │   │
│                            │  │     ★★★★☆                               │   │
│                            │  │                                      │   │
│                            │  │  ❹ 探索效率                              │   │
│                            │  │     维度⑥ │ 84%算力浪费在非关键步骤     │   │
│                            │  │     （CSO反推：仅16%步骤关键）           │   │
│                            │  │     ★★★☆☆                               │   │
│                            │  │                                      │   │
│                            │  │  ❺ 稀疏延迟奖励的归因                    │   │
│                            │  │     维度⑤ │ 轨迹级奖励太粗             │   │
│                            │  │     无法区分"碰巧做对"和"真正学会"       │   │
│                            │  │     ★★★☆☆                               │   │
│                            │  └──────────────────────────────────────┘   │
│                            │                                             │
│                            │  ┌─ ⚪横切：数值可信 ─────────────────┐     │
│                            │  │  穿透全部五个挑战                    │     │
│                            │  │  RL对数值噪声极敏感，底层不可信      │     │
│                            │  │  上层调参就变玄学                    │     │
│                            │  └──────────────────────────────────┘     │
│                            │                                             │
├────────────────────────────┴─────────────────────────────────────────────┤
│  【底部过渡条（高亮）】                                                     │
│  这5个挑战驱动后训练软件栈三层架构全面升级 → P4展开                        │
└──────────────────────────────────────────────────────────────────────────┘
```

**视觉设计要点**：
- 右半页不按层分组，五个挑战平铺展示，每个挑战一行
- 每行三要素：挑战名称、来源维度编号、量化严重性数据+星级标注
- 横切约束用白色/灰色边框横跨底部，表示穿透全部五个挑战
- 左半页对比表七维不分组——维度是原因，挑战是结果，因果箭头直接指向右侧
- 挑战不预设分层，分层是P4解法的组织方式
- 底部过渡句明确"5挑战驱动三层架构升级"，为P4搭桥

### 演讲话术（60-90秒）

> "上一页我们说，直接把LLM RL搬到Agent场景会结构性失配。失配在哪？我把七个维度摊开，直接正推出五个挑战加一个横切约束。
>
> 左边七维对比，右边五个挑战，逐个看。
>
> 第一，长序列推理开销。维度一算力加维度七上下文联合驱动——Agent会话百万级token，单会话KV Cache数个GB，千并发直接撑爆显存。70B模型不量化单卡放不下。这是最高频的痛点。
>
> 第二，环境交互延迟。维度二环境加维度三数据驱动——Agent的Episode动辄分钟级，工具调用的长尾延迟拖拽整条rollout。
>
> 第三，在线数据吞吐瓶颈。维度四驱动——训推分离后数据天然异步产生，版本差超过4步就发散，SkyRL实测数据。
>
> 第四，探索效率。维度六驱动——84%的rollout算力浪费在非关键步骤上，CSO的研究表明仅16%步骤是关键的。
>
> 第五，稀疏延迟奖励的归因。维度五驱动——轨迹级奖励太粗，分钟级轨迹里到底哪一步起了作用？无法区分'碰巧做对'和'真正学会'。
>
> 最后，横切约束——数值可信。穿透全部五个挑战。RL对数值噪声极敏感，底层精度不可信，上层一切调参都是玄学。
>
> 底部这句话最关键：**这五个挑战驱动后训练软件栈三层架构全面升级**。下一页展开。"

---

## P4 | 五挑战→七升级方向的因果传导 + 三层架构

### PPT内容

**标题**：五挑战驱动七个架构升级方向——后训练软件栈三层全面升级

**核心信息**：上半页用因果传导表展示5个挑战如何驱动7个升级方向（每行一个升级方向，列为"驱动挑战|升级方向|因为→所以→否则|对应技术"）；下半页用三层架构图展示升级方向和技术的层级归属。逻辑从"挑战→必须升级→否则后果→对应技术→落在哪层"。

**P3→P4 承接**：P3 推导出五挑战+横切，P4 回答"这些挑战驱动哪些架构升级？每个升级为什么是必然的？业界给出了什么方案？"

```
┌──────────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                          │
│  标题：五挑战→七升级方向——后训练软件栈三层全面升级                          │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  【上半页：5挑战→7升级方向 因果传导表】                                   │
│                                                                          │
│  ┌──────────┬─────────────────┬───────────────────────────────┬─────────┐│
│  │驱动挑战   │升级方向          │因为→所以→否则                  │对应技术  ││
│  ├──────────┼─────────────────┼───────────────────────────────┼─────────┤│
│  │❶长序列   │a)KV Cache       │因为Agent会话KV数GB→所以必须    │LMCache  ││
│  │推理开销  │  分布式池化      │跨节点池化KV→否则千并发撑爆显存  │Mooncake ││
│  │+        ├─────────────────┼───────────────────────────────┼─────────┤│
│  │❷环境    │b)推理加速        │因为70B不量化单卡放不下→所以必须 │Eagle3   ││
│  │交互延迟  │                 │量化+投机推理→否则推理成本不可控  │4bit量化 ││
│  │         ├─────────────────┼───────────────────────────────┼─────────┤│
│  │         │c)环境sandbox    │因为Episode分钟级→所以必须并行   │Forge    ││
│  │         │  并行化         │sandbox→否则rollout吞吐瓶颈      │         ││
│  ├──────────┼─────────────────┼───────────────────────────────┼─────────┤│
│  │❷环境    │d)训推解耦        │因为环境I/O阻塞GPU→所以必须     │ProRL    ││
│  │交互延迟  │  (Rollout服务化) │Rollout独立服务→否则GPU空转      │Agent    ││
│  │+        ├─────────────────┼───────────────────────────────┼─────────┤│
│  │❸在线    │e)异步流水线+     │因为数据在线流式产生→所以必须    │Dynamo   ││
│  │数据吞吐  │  动态调度        │异步调度→否则训练等待推理空转     │         ││
│  ├──────────┼─────────────────┼───────────────────────────────┼─────────┤│
│  │❹探索    │f)off-policy容忍 │因为异步数据天然过时→所以必须    │Decoupled││
│  │效率      │  +多样性探索     │IS修正+探索鼓励→否则训练发散    │PPO/DAPO ││
│  │+        │                 │                               │+CISPO  ││
│  │❸在线    │                 │                               │         ││
│  │数据吞吐  │                 │                               │         ││
│  ├──────────┼─────────────────┼───────────────────────────────┼─────────┤│
│  │❺稀疏    │g)步级奖励+      │因为轨迹级奖励太粗→所以必须     │Cursor   ││
│  │延迟奖励  │  精细归因        │step-level归因→否则84%算力浪费  │实时RL+  ││
│  │归因      │                 │在非关键步骤                    │Meta SSR ││
│  ├──────────┴─────────────────┴───────────────────────────────┴─────────┤│
│  │ ⚪横切：数值精度保证 → 训推一致性/确定性计算/精度基线/算子自动生成       ││
│  └───────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  【下半页：三层架构图】                                                    │
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  🔵 基础设施层（挑战❶+❷驱动）                                       │  │
│  │  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐         │  │
│  │  │ a) KV Cache     │ │ b) 推理加速     │ │ c) sandbox     │         │  │
│  │  │ 分布式池化       │ │ Eagle3/4bit量化 │ │ 并行化         │         │  │
│  │  │ LMCache/Mooncake│ │                │ │ Forge          │         │  │
│  │  └────────────────┘ └────────────────┘ └────────────────┘         │  │
│  ├───────────────────────────────────────────────────────────────────┤  │
│  │  🟢 框架层（挑战❷+❸驱动）                                          │  │
│  │  ┌────────────────────────┐ ┌────────────────────────┐            │  │
│  │  │ d) 训推解耦             │ │ e) 异步流水线+动态调度  │            │  │
│  │  │ Rollout服务化           │ │ Dynamo                 │            │  │
│  │  │ ProRL Agent            │ │                        │            │  │
│  │  └────────────────────────┘ └────────────────────────┘            │  │
│  ├───────────────────────────────────────────────────────────────────┤  │
│  │  🟠 算法层（挑战❹+❺驱动）                                          │  │
│  │  ┌────────────────────────┐ ┌────────────────────────┐            │  │
│  │  │ f) off-policy容忍+     │ │ g) 步级奖励+精细归因    │            │  │
│  │  │ 多样性探索              │ │ Cursor实时RL+Meta SSR  │            │  │
│  │  │ Decoupled PPO/DAPO     │ │                        │            │  │
│  │  │ +CISPO                 │ │                        │            │  │
│  │  └────────────────────────┘ └────────────────────────┘            │  │
│  ├───────────────────────────────────────────────────────────────────┤  │
│  │  ⚪ 横切：数值精度保证                                               │  │
│  │  训推一致性 / 确定性计算 / 精度基线 / 算子自动生成                     │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  底部（高亮）：                                                            │
│  五挑战驱动七升级方向，每个升级都有"因为→所以→否则"的因果必然性              │
│  → 三层架构全面升级×全栈协同是唯一出路                                     │
│  → 接下来逐层展开，先看基础设施层                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

**视觉设计要点**：
- 上半页因果传导表：每行一个升级方向，四列（驱动挑战|升级方向|因为→所以→否则|对应技术）
- 因果链用"因为→所以→否则"三段式，让每个升级方向的必然性一目了然
- 下半页三层架构图：基础设施层(a/b/c)在最上层、框架层(d/e)在中间、算法层(f/g)在最下层
- 每层标注驱动挑战编号和对应技术
- 横切约束横跨底部穿透三层
- 色标与P3对应：基础设施层蓝色、框架层绿色、算法层橙色

### 演讲话术（60-90秒）

> "P3的五个挑战不需要背下来。关键看它们驱动了什么。这一页回答：**每个挑战为什么必然驱动特定的架构升级？不升级会怎样？**
>
> 先看上半页因果传导表。每一行一个升级方向，关键看第三列——'因为→所以→否则'。
>
> 基础设施层三个方向。第一，KV Cache分布式池化——因为Agent会话KV数GB，所以必须跨节点池化，否则千并发撑爆显存。第二，推理加速——因为70B不量化单卡放不下，所以必须Eagle3投机推理加4bit量化，否则推理成本不可控。第三，sandbox并行化——因为Episode分钟级，所以必须并行sandbox，否则rollout吞吐瓶颈。
>
> 框架层两个方向。训推解耦——因为环境I/O阻塞GPU，所以必须Rollout独立服务，否则GPU空转。异步流水线加动态调度——因为数据在线流式产生，所以必须异步调度，否则训练等推理空转。
>
> 算法层两个方向。off-policy容忍加多样性探索——因为异步数据天然过时，所以必须IS修正加探索鼓励，否则训练发散。步级奖励加精细归因——因为轨迹级奖励太粗，所以必须step-level归因，否则84%算力浪费在非关键步骤。
>
> 最后横切——数值精度保证穿透三层：训推一致性、确定性计算、精度基线、算子自动生成。
>
> 下半页架构图：七个方向落在三层，每层都有明确的驱动挑战和对应技术。**五挑战驱动七升级，每个升级都有因果必然性——不做会出什么问题写得清清楚楚。** 接下来逐层展开。"

---

## P5 | 基础设施层升级：KV池化+推理加速+sandbox并行（挑战❶+❷驱动）

### PPT内容

**标题**：基础设施层升级：KV池化+推理加速+sandbox并行（挑战❶+❷驱动）

```
┌─────────────────────────────────────────────────────────────────────┐
│  [深色渐变头部条]                                                     │
│  标题：Reply环节：策略时效性与奖励归因（C1+C4）                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区（左侧 60%）             张力示意（右侧 40%）      │     │
│  │                                                            │     │
│  │  Reply环节承载两个挑战：       ┌────────┐  ←对抗→ ┌──────┐│     │
│  │                                 │ 吞吐量  │         │新鲜度││     │
│  │  C1 策略时效性：                 │ ↑↑↑↑   │         │ ↑↑↑ ││     │
│  │  训推分离让推理集群全速跑        └────────┘         └──────┘│     │
│  │  rollout，但 on-policy RL                                  │     │
│  │  要求数据来自最新策略。            ┌────────┐  ←对抗→ ┌──────┐│     │
│  │  Agent episode 动辄几分钟，       │ 归因    │         │效率  ││     │
│  │  陈旧度被放大。                   │ 精度    │         │      ││     │
│  │                                  └────────┘         └──────┘│     │
│  │  C4 奖励与归因：                                             │     │
│  │  84%算力浪费在非关键步骤，                                   │     │
│  │  轨迹级奖励太粗，归因困难。                                   │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ Off-Policy基础层   │ │ 工业级RL算法层    │ │ 闭环训练+数据层   │    │
│  │                  │ │                  │ │                  │    │
│  │ Decoupled PPO    │ │ DAPO+CISPO       │ │ Cursor实时RL +   │    │
│  │ (Off-Policy PPO) │ │ ByteDance+MiniMax│ │ Meta Self-Play   │    │
│  │                  │ │                  │ │ SWE-RL           │    │
│  │ actor-learner    │ │ DAPO四大技术+    │ │                  │    │
│  │ 物理分离+V-trace │ │ CISPO IS裁剪     │ │ 极快闭环缩短     │    │
│  │ 截断IS修正+      │ │ 建立在Off-Policy │ │ horizon+自博弈   │    │
│  │ IW裁剪           │ │ PPO基础之上       │ │ 无限数据生成     │    │
│  │ →应对C1         │ │ →应对C1         │ │ →应对C1,C4      │    │
│  │                  │ │                  │ │                  │    │
│  │ → P6 详细展开    │ │ → P7 详细展开    │ │ → P8 详细展开    │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "现在我们沿R3数据流逐层展开。第一站：Reply环节——回放更新，这是算法层的主战场。
>
> 这个环节承载两个挑战：C1策略时效性和C4奖励与归因。
>
> 先看C1。请看右上角这个对抗关系——吞吐量和新鲜度不可兼得。训推分离让推理集群全速跑 rollout，但 on-policy RL 要求数据来自最新策略。Agent 的 Episode 动辄几分钟，比 chatbot 长一到两个量级，陈旧度被极度放大。
>
> 再看C4。右下角第二个对抗——归因精度和效率不可兼得。84%的rollout算力浪费在非关键步骤上，轨迹级奖励太粗，无法区分'碰巧做对'和'真正学会'。
>
> 怎么解？请看下面三个卡片，从基础到改进到实践层层递进。第一，Off-Policy基础层——Decoupled PPO，异步架构下rollout数据天然off-policy，actor-learner物理分离加V-trace截断IS修正是所有异步RL的底层基础。第二，工业级RL算法层——DAPO+CISPO，建立在Off-Policy PPO基础之上，DAPO四大技术加CISPO的IS权重裁剪，已成开源RL事实标准。第三，闭环训练加数据层——Cursor实时RL做5小时极快闭环缩短horizon，Meta自博弈生成无限训练数据——同时应对C1和C4。
>
> 三个技术从off-policy基础到工业级算法到训练闭环，层层递进。接下来三页逐一展开。"

---

## P6 | 技术①-1：Decoupled PPO（Off-Policy PPO）

### PPT内容

**标题**：Decoupled PPO：异步架构的Off-Policy基础——actor-learner分离+V-trace修正
**标签**：Reply环节 · C1策略时效性 · Off-Policy基础层

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Decoupled PPO：异步架构的Off-Policy基础                         │
│  标签：Reply环节 · C1策略时效性 · Off-Policy基础层                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】方案        │ │
│  │                                      │  │                     │ │
│  │  异步架构下rollout数据天然off-policy：  │  │  ● actor-learner   │ │
│  │  - actor生成数据时用的是旧策略          │  │    物理分离          │ │
│  │  - learner更新时策略已经变了           │  │    推理集群(actor)   │ │
│  │  - importance ratio失真              │  │    独立于训练集群    │ │
│  │  - 策略版本差>4步直接发散              │  │    (learner)        │ │
│  │    (SkyRL实测数据)                    │  │                     │ │
│  │                                      │  │  ● V-trace截断IS   │ │
│  │  这是所有异步RL的基础问题——            │  │    修正              │ │
│  │  不解决off-policy，上层所有算法         │  │    截断重要性权重    │ │
│  │  都建立在不稳定的地基上               │  │    ρ̄=min(ρ,c̄)      │ │
│  │                                      │  │    防止极端ratio     │ │
│  │  ┌────────────────────────┐          │  │    导致梯度爆炸      │ │
│  │  │ 问题示意：              │          │  │                     │ │
│  │  │ Actor(v_old) → 数据    │          │  │  ● importance       │ │
│  │  │ Learner(v_new) ← 学习  │          │  │    weight裁剪       │ │
│  │  │ v_old ≠ v_new → 偏差   │          │  │    控制off-policy   │ │
│  │  └────────────────────────┘          │  │    数据的权重上界    │ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：这是所有异步RL的基础——DAPO和CISPO都建立在           │    │
│  │  Off-Policy PPO的actor-learner分离+IS修正之上。                │    │
│  │  不理解这个基础，就无法理解后续工业级算法为什么这样设计。         │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU的RL kernel需原生支持off-policy importance weight   │    │
│  │  计算、V-trace修正算子、截断IS权重裁剪——这是异步RL的底层能力    │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "Reply环节的第一个技术，也是最基础的一个——Decoupled PPO，或者叫Off-Policy PPO。为什么把它放在第一个？因为这是所有异步RL的地基。
>
> 请看左侧问题——异步架构下，actor生成rollout数据时用的是旧策略，learner更新参数时策略已经变了。这就导致rollout数据天然是off-policy的，importance ratio失真。SkyRL的实测数据显示，策略版本差超过4步就直接训练发散。
>
> 这不是某个具体算法的问题，而是所有异步RL的结构性问题——不解决off-policy，上层所有算法都建立在不稳定的地基上。
>
> 右侧是三个核心机制。第一，actor-learner物理分离——推理集群独立于训练集群，各自全速运行。第二，V-trace截断IS修正——用截断的重要性权重防止极端ratio导致梯度爆炸。第三，importance weight裁剪——控制off-policy数据的权重上界，保证训练稳定性。
>
> 底部关键逻辑：DAPO和CISPO都建立在这个Off-Policy PPO的基础之上。不理解actor-learner分离和IS修正，就无法理解后续工业级算法为什么那样设计。对我们的启示——NPU的RL kernel需原生支持off-policy importance weight计算和V-trace修正算子。"

---

## P7 | 技术①-2：DAPO+CISPO

### PPT内容

**标题**：DAPO+CISPO：建立在Off-Policy基础上的工业级异步RL算法
**标签**：Reply环节 · C1策略时效性 · 工业级RL算法层 · ByteDance Seed + MiniMax

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：DAPO+CISPO：建立在Off-Policy基础上的工业级异步RL算法             │
│  标签：Reply环节 · C1策略时效性 · 工业级RL算法层 · ByteDance + MiniMax│
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】方案        │ │
│  │                                      │  │                     │ │
│  │  有了Off-Policy PPO的基础之后，       │  │  ● DAPO四大技术     │ │
│  │  工业级大规模异步RL还有四个痛点：       │  │    Clip-Higher /   │ │
│  │  - 裁剪比率不对称导致探索坍缩          │  │    动态采样 /        │ │
│  │  - 动态采样效率低                     │  │    Token级梯度 /    │ │
│  │  - token级梯度缺失                   │  │    超长奖励整形      │ │
│  │  - 超长奖励序列信号衰减               │  │                     │ │
│  │                                      │  │  ● CISPO(MiniMax)   │ │
│  │  在Agent场景(200K+上下文)             │  │    对IS权重裁剪，    │ │
│  │  这些问题被极度放大                   │  │    比DAPO快2x        │ │
│  │                                      │  │    驱动M2.5达       │ │
│  │  ┌────────────────────────┐          │  │    SWE-Bench 80.2% │ │
│  │  │ 问题示意：              │          │  │                     │ │
│  │  │ PPO clip: ε=0.2 对称   │          │  │  ● DAPO已集成       │ │
│  │  │ 探索方向被过度裁剪      │          │  │    OpenRLHF，成为   │ │
│  │  │ → 策略过早收敛          │          │  │    开源RL事实标准    │ │
│  │  └────────────────────────┘          │  │                     │ │
│  │                                      │  │  ● AIME 50分(32B)   │ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：在Decoupled PPO的off-policy修正基础上，DAPO+CISPO  │    │
│  │  进一步解决裁剪、采样、梯度三大工业级问题                       │    │
│  │  已成工业界共识，不是学术探索而是生产级验证                      │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU的RL kernel需原生支持DAPO/CISPO的token级梯度计算、  │    │
│  │  动态采样过滤、IS权重裁剪等算子                                │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "有了Off-Policy PPO的基础，接下来看建立在它之上的工业级算法——DAPO来自ByteDance Seed，CISPO来自MiniMax。
>
> 请看左侧问题——off-policy修正解决了异步数据的分布偏差，但工业级大规模RL还有四个更具体的痛点：裁剪比率不对称导致探索坍缩、动态采样效率低、token级梯度缺失、超长奖励序列信号衰减。在Agent场景下，200K以上的上下文长度让这些问题被极度放大。
>
> 右侧是两套互补方案。DAPO提出四大技术——Clip-Higher打破不对称裁剪、动态采样提升效率、Token级梯度精细化更新、超长奖励整形解决信号衰减。32B模型在AIME上达到50分。DAPO已经集成到OpenRLHF，成为2026年开源RL的事实标准。
>
> CISPO是MiniMax的方案，核心是对重要性采样权重做更精细的裁剪，在200K上下文、百万级日吞吐下验证过，比DAPO快2倍。驱动MiniMax M2.5在SWE-Bench达到80.2%。
>
> 底部关键逻辑：DAPO+CISPO是在Decoupled PPO的off-policy修正基础上，进一步解决裁剪、采样、梯度三大工业级问题。从学术探索走向工业共识。对我们的启示——NPU的RL kernel需要原生支持token级梯度计算、动态采样过滤和IS权重裁剪等算子。"

---

## P8 | 技术①-3：Cursor实时RL + Meta Self-Play SWE-RL

### PPT内容

**标题**：Cursor实时RL + Meta SSR：极快闭环 × 自博弈，两条互补路径解决C1+C4
**标签**：Reply环节 · C1策略时效性+C4奖励与归因 · 闭环训练+数据层

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Cursor实时RL + Meta SSR：极快闭环 × 自博弈                      │
│  标签：Reply环节 · C1+C4 · 闭环训练+数据层                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】两个问题                     │  │ 【右侧】两条互补路径 │ │
│  │                                      │  │                     │ │
│  │  问题1（C1）：                        │  │  ● Cursor实时RL     │ │
│  │  闭环太慢——传统RL训练闭环             │  │    Anyrun平台       │ │
│  │  cycle数小时甚至数天，                │  │    10万+沙箱环境     │ │
│  │  策略更新跟不上探索速度               │  │    5小时端到端闭环   │ │
│  │                                      │  │    日内多次部署到    │ │
│  │  问题2（C4）：                        │  │    数百万用户        │ │
│  │  数据太贵——Agent训练数据              │  │    → 极快闭环缩短   │ │
│  │  需要真实环境交互轨迹，               │  │      horizon        │ │
│  │  人工标注成本极高，                   │  │    (Cursor的实时RL  │ │
│  │  规模受限                            │  │     也利用环境反馈   │ │
│  │                                      │  │     获取过程信号)    │ │
│  │  ┌────────────────────────┐          │  │                     │ │
│  │  │ 瓶颈示意：              │          │  │  ● Meta Self-Play   │ │
│  │  │ 传统：train→eval→fix   │          │  │    SWE-RL (SSR)     │ │
│  │  │       [数小时/数天]     │          │  │    自博弈生成无限   │ │
│  │  │ Cursor: 5小时端到端    │          │  │    训练数据          │ │
│  │  └────────────────────────┘          │  │    零人工标注        │ │
│  │                                      │  │    SWE-Bench         │ │
│  │                                      │  │    Verified +10.4分  │ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：Cursor缩短horizon解决C1，Meta扩展数据解决C4       │    │
│  │  两条路互补：一个做快，一个做大                                │    │
│  │  注：Cursor的密集闭环天然包含环境反馈的过程信号，               │    │
│  │  部分替代了独立过程奖励模型的需求                               │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU需支持高并发沙箱RL闭环能力和自博弈数据生成管线，      │    │
│  │  这是Agent RL训练的基础能力                                   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "Decoupled PPO解决了off-policy基础，DAPO+CISPO解决了工业级算法问题。但还有两个更上层的痛点——闭环太慢和数据太贵。Cursor和Meta各走了一条路，刚好互补。
>
> 先看Cursor的实时RL——核心是极快闭环。Anyrun平台同时运行10万以上沙箱环境，5小时端到端RL闭环，日内多次部署到数百万用户。他们把horizon压短了——不是让Agent跑完整条长轨迹再学，而是密集短循环快速迭代。这直接应对C1策略时效性。Composer 2上线后，用RL训练的self-summarization把压缩错误降了50%。值得注意的是，Cursor的密集闭环天然包含来自环境反馈的过程信号，部分替代了独立过程奖励模型的需求。
>
> 再看Meta的Self-Play SWE-RL，核心是零人工标注的无限数据。自博弈范式让模型自己生成问题、自己解决、自己评判。训练规模不再受标注瓶颈限制。在SWE-Bench Verified上提升10.4分。这直接应对C4——数据量上去了，归因信号的统计质量自然提高。
>
> 关键逻辑：Cursor缩短horizon做快，Meta扩展数据做大，两条路互补。对我们的启示——NPU需支持高并发沙箱RL闭环能力和自博弈数据生成管线。
>
> Reply环节小结：三个技术层层递进——Decoupled PPO做off-policy基础、DAPO+CISPO做工业级算法改进、Cursor+Meta做闭环训练和数据生成。接下来沿R3数据流进入Routing环节。"

---

## P9 | 框架层升级：训推解耦+异步调度（挑战❷+❸驱动）

### PPT内容

**标题**：框架层升级：训推解耦+异步调度（挑战❷+❸驱动）

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Routing环节：采集吞吐与推理效率（C2+C3）                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区                          张力示意               │     │
│  │                                                            │     │
│  │  训推分离把环境交互全放到           ┌────────┐ ←→ ┌──────┐ │     │
│  │  推理侧。四重复杂度叠加：          │有状态   │     │高吞吐│ │     │
│  │                                   └────────┘     └──────┘ │     │
│  │  ① Rollout与训练耦合，环境I/O阻塞GPU                       │     │
│  │  ② 环境并发管理（千级sandbox）                               │     │
│  │  ③ KV cache 局部性 vs 调度灵活性                            │     │
│  │  ④ 工具调用长尾延迟拖拽整条rollout                           │     │
│  │                                                            │     │
│  │  单独看都能解，叠在一起不是线性增长的复杂度                    │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐ ┌────────────────┐ │
│  │ Rollout解耦层   │ │ 推理调度层      │ │ 存储/缓存层    │ │ 分离架构层     │ │
│  │                │ │                │ │                │ │                │ │
│  │ ProRL Agent    │ │ NVIDIA Dynamo  │ │ LMCache        │ │ Mooncake       │ │
│  │ NVIDIA         │ │                │ │                │ │ 月之暗面       │ │
│  │                │ │ KV感知路由，   │ │ 跨引擎P2P共享 │ │ KV Cache分离   │ │
│  │ Rollout-as-a-  │ │ 请求去已有缓存│ │ KV cache，打破│ │ 跨节点池化     │ │
│  │ Service三阶段  │ │ 的节点        │ │ 节点孤岛      │ │ RDMA+分布式    │ │
│  │ 异步流水线     │ │               │ │               │ │ KV Store       │ │
│  │ →应对C1,C2    │ │ → P11详细展开 │ │ → P12详细展开 │ │ → P13详细展开  │ │
│  │               │ │               │ │               │ │                │ │
│  │ → P10详细展开 │ │               │ │               │ │                │ │
│  └────────────────┘ └────────────────┘ └────────────────┘ └────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "R3数据流的第二站：Routing环节——路由请求，这是框架层的主战场，技术密度最高，四项技术集中在这里。
>
> 这个环节承载两个挑战：C2采集吞吐和C3推理效率。
>
> 请看右上角这个对抗关系——有状态和高吞吐不可兼得。训推分离把环境交互全放到推理侧，四重复杂度叠加。
>
> 第一，Rollout和训练的耦合——环境I/O阻塞GPU训练，这是框架级的架构问题。第二，环境并发管理——千级 sandbox 同时运行，C2采集吞吐的核心痛点。第三，KV cache 局部性和调度灵活性的矛盾。第四，70B模型不量化单卡放不下，工具调用的长尾延迟拖拽整条 rollout——C3推理效率的核心痛点。
>
> 下面四个卡片。Rollout解耦层，NVIDIA ProRL Agent做Rollout-as-a-Service——注意，这项技术虽然也缓解Reply环节的C1策略时效性，但它本质是框架级的架构解耦，所以归在Routing框架层更准确。推理调度层，Dynamo做KV感知路由。存储缓存层，LMCache做跨引擎P2P共享KV cache。分离架构层，Mooncake用RDMA+分布式KV Store实现prefill-decode分离和KV缓存池化。接下来四页逐一展开。"

---

## P10 | 技术②-1：ProRL Agent

### PPT内容

**标题**：ProRL Agent：Rollout-as-a-Service，NVIDIA终结耦合式Agent RL
**标签**：Routing环节 · C1+C2 · Rollout解耦层 · NVIDIA（2026.03发布）

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：ProRL Agent：Rollout-as-a-Service，终结耦合式Agent RL         │
│  标签：Routing环节 · C1+C2 · Rollout解耦层 · NVIDIA（2026.03）      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】方案        │ │
│  │                                      │  │                     │ │
│  │  传统Agent RL（如SkyRL/VeRL-Tool）   │  │  ● Rollout-as-a-   │ │
│  │  把Rollout耦合在Trainer内：           │  │    Service架构      │ │
│  │  - 环境I/O阻塞GPU训练               │  │    三阶段异步流水线  │ │
│  │  - GPU空等环境返回结果               │  │    INIT/RUN/EVAL    │ │
│  │  - 多轮Agent交互延迟累积             │  │                     │ │
│  │                                      │  │  ● 集成NeMo Gym    │ │
│  │  这是框架级的架构问题——               │  │    支持Singularity  │ │
│  │  不是算法能修的，必须从框架层解耦     │  │    沙箱环境          │ │
│  │                                      │  │                     │ │
│  │  ┌────────────────────────┐          │  │  ● Token ID全链路   │ │
│  │  │ 耦合式瓶颈示意：        │          │  │    透传，策略版本    │ │
│  │  │ GPU: [训练][等][训练][等]│          │  │    精确追踪          │ │
│  │  │      ↑环境I/O阻塞GPU    │          │  │                     │ │
│  │  └────────────────────────┘          │  │  ● Qwen3-8B:       │ │
│  │                                      │  │    SWE-Bench        │ │
│  │                                      │  │    9.6%→18.0%(近2x)│ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：Rollout必须从Trainer中解耦为独立服务，才能释放GPU   │    │
│  │  这是框架级解耦，不是算法修正——归在Routing层更准确             │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU训练框架需支持Rollout-as-a-Service解耦架构，        │    │
│  │  环境交互不阻塞训练计算，Token ID全链路透传                    │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "Routing环节的第一个技术——ProRL Agent，NVIDIA 2026年3月发布。为什么把它归在Routing框架层而不是Reply算法层？因为它解决的本质问题是架构耦合，不是算法修正。
>
> 请看左侧问题——NVIDIA论文直接点名了SkyRL和VeRL-Tool等传统方案的耦合缺陷：Rollout耦合在Trainer内，GPU空等环境返回结果，多轮Agent交互延迟层层累积。这不是调算法能修的，必须从框架层做架构解耦。
>
> 右侧是NVIDIA的方案：Rollout-as-a-Service——把Rollout提升为独立服务，三阶段异步流水线INIT/RUN/EVAL完全解耦。集成NeMo Gym，支持Singularity沙箱环境。关键设计是Token ID全链路透传，策略版本精确追踪。
>
> 实测结果：Qwen3-8B在SWE-Bench从9.6%提升到18.0%，接近2倍。
>
> 关键逻辑：这是框架级解耦——同时缓解Reply环节的C1策略时效性（数据更新鲜）和Routing环节的C2采集吞吐（推理集群不被训练阻塞）。对我们的启示——NPU训练框架需支持Rollout-as-a-Service解耦架构。"

---

## P11 | 技术②-2：NVIDIA Dynamo

### PPT内容

**标题**：NVIDIA Dynamo：KV 感知路由，让请求去已有缓存的节点
**标签**：Routing环节 · C2采集吞吐 · 推理调度层

**问题**：Agent 多轮对话中大量 prefix 可复用，但标准负载均衡不知道哪个节点有哪段 KV cache，导致重复计算。

**方案**：
- NVIDIA GTC 2025 发布，开源推理框架
- KV-aware Router：Radix Tree 追踪全集群 KV 分布，计算 cache overlap score
- NIXL：点对点 KV cache 传输库
- KV Block Manager：多层级内存管理（GPU VRAM → CPU RAM → SSD）
- Prefill-Decode 分离

**关键逻辑**：推理调度从"哪个节点空闲"变成"哪个节点已有我需要的 KV"

**启示**：NPU 推理引擎需要支持 KV cache 元数据暴露和跨节点迁移能力

### 演讲话术（60-90秒）

> "NVIDIA Dynamo，GTC 2025 发布的开源推理框架，解决推理调度层的问题。
>
> 核心问题：Agent 多轮对话大量 prefix 可复用，但标准负载均衡只看哪个节点空闲，不知道哪个节点有我需要的 KV cache。结果是同一段上下文被不同节点重复计算。
>
> Dynamo 的方案是 KV-aware Router——用 Radix Tree 追踪全集群 KV 分布，计算 cache overlap score，把请求路由到已有缓存的节点。配合 NIXL 点对点传输库和多层级内存管理，实现 Prefill-Decode 分离。
>
> 关键逻辑：推理调度从'哪个节点空闲'变成'哪个节点已有我需要的 KV'。对我们的启示——NPU 推理引擎需要支持 KV cache 元数据暴露和跨节点迁移能力，这是 Agent 推理场景的刚需。"

---

## P12 | 技术②-3：LMCache

### PPT内容

**标题**：LMCache：跨引擎 P2P 共享 KV cache，打破节点孤岛
**标签**：Routing环节 · C2+C3 · 存储缓存层

**问题**：每个推理引擎独立维护 KV cache，同一 prefix 被多个引擎重复计算。Agent 场景 95%+ context 可复用。

**方案**：
- 开源项目，集成 vLLM、SGLang、KServe
- P2P 共享：引擎间直接传输 KV cache
- Cache Offloading：GPU VRAM → CPU RAM 多级卸载
- Connector 接口：标准化 KV cache 存取 API
- 实测：多轮 QA 15x 吞吐提升，TTFT 降低 54.7%

**关键逻辑**：KV cache 不应是引擎私有资源，而应是集群级的共享池

**启示**：NPU 推理软件栈需支持标准化 KV cache 存取接口和高效跨节点传输

### 演讲话术（60-90秒）

> "Dynamo 解决路由——把请求送到正确的节点。LMCache 解决的是更底层的问题——KV cache 本身怎么共享。
>
> 问题很直观：每个推理引擎独立维护自己的 KV cache，同一段 prefix 被多个引擎重复计算。Agent 场景下 95% 以上的 context 可以复用，重复计算的浪费是巨大的。
>
> LMCache 是开源项目，集成了 vLLM、SGLang、KServe。核心是 P2P 共享——引擎间直接传输 KV cache，加上 GPU VRAM 到 CPU RAM 的多级卸载，和标准化的 KV cache 存取 API。
>
> 实测结果：多轮 QA 场景 15 倍吞吐提升，首 token 延迟降低 54.7%。
>
> 关键逻辑是：KV cache 不应是引擎私有资源，而应是集群级的共享池。对我们的启示——NPU 推理软件栈需要支持标准化 KV cache 存取接口和高效跨节点传输。"

---

## P13 | 算法层升级：off-policy容忍+步级归因（挑战❹+❺驱动）

### PPT内容

**标题**：算法层升级：off-policy容忍+步级归因（挑战❹+❺驱动）
**标签**：算法层 · 挑战❹探索效率+挑战❺稀疏延迟奖励归因

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Mooncake：KV Cache分离架构的生产级实践                          │
│  标签：Routing环节 · C2+C3 · 推理架构层 · 月之暗面（Moonshot AI）     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌─────────────────────┐ │
│  │  【左侧】问题                         │  │  【右侧】三大机制    │ │
│  │                                      │  │                     │ │
│  │  Agent长程对话场景下，KV Cache:        │  │ ┌─────────────────┐│ │
│  │  - 单会话可达数GB                     │  │ │ KVCache-centric  ││ │
│  │  - 千并发撑爆GPU显存                  │  │ │ Disaggregated    ││ │
│  │  - Prefill和Decode对资源需求          │  │ │ Architecture     ││ │
│  │    截然不同（计算密集 vs 访存密集）     │  │ │                  ││ │
│  │                                      │  │ │ 以KV Cache为中心  ││ │
│  │  传统方案把KV Cache绑定在             │  │ │ 做prefill-decode  ││ │
│  │  生成它的GPU上——                     │  │ │ 分离调度          ││ │
│  │  扩展性受单机显存硬约束               │  │ └─────────────────┘│ │
│  │                                      │  │ ┌─────────────────┐│ │
│  │  ┌────────────────────────┐          │  │ │ RDMA + 分布式    ││ │
│  │  │ 瓶颈示意：              │          │  │ │ KV Store         ││ │
│  │  │ GPU_A: [KV][KV][KV]满 │          │  │ │                  ││ │
│  │  │ GPU_B: [  ][  ][  ]空 │          │  │ │ KV Cache通过RDMA ││ │
│  │  │        ↑ 无法迁移       │          │  │ │ 跨节点传输，存入  ││ │
│  │  └────────────────────────┘          │  │ │ 分布式KV Store   ││ │
│  │                                      │  │ │ 解耦存储与计算   ││ │
│  │                                      │  │ └─────────────────┘│ │
│  │                                      │  │ ┌─────────────────┐│ │
│  │                                      │  │ │ Prefill-Decode   ││ │
│  │                                      │  │ │ 分离             ││ │
│  │                                      │  │ │                  ││ │
│  │                                      │  │ │ Prefill节点专做  ││ │
│  │                                      │  │ │ 计算密集的首次   ││ │
│  │                                      │  │ │ 编码，Decode节点 ││ │
│  │                                      │  │ │ 专做访存密集的   ││ │
│  │                                      │  │ │ 逐token生成      ││ │
│  │                                      │  │ └─────────────────┘│ │
│  └──────────────────────────────────────┘  └─────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑：月之暗面Kimi的生产系统验证了KV Cache分离架构         │    │
│  │  的可行性——不是学术概念，是真实线上流量验证过的方案              │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU推理架构需支持KV Cache分离和跨节点池化，              │    │
│  │       HCCS是天然优势                                         │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**问题**：Agent长程对话场景下，单会话KV Cache可达数GB，千并发撑爆GPU显存。传统方案KV Cache绑定在生成它的GPU上，无法跨节点迁移，扩展性受单机显存硬约束。

**方案**：
- 月之暗面（Moonshot AI）Kimi生产系统的推理架构
- KVCache-centric Disaggregated Architecture：以KV Cache为中心重新设计推理调度
- RDMA + 分布式KV Store：KV Cache通过RDMA跨节点传输，存入分布式KV Store，解耦存储与计算
- Prefill-Decode分离：Prefill节点专做计算密集的首次编码，Decode节点专做访存密集的逐token生成
- 已在Kimi线上生产环境验证，不是学术概念

**关键逻辑**：KV Cache分离架构是解决有状态推理扩展性的生产级方案，已经过真实流量验证

**启示**：NPU推理架构需支持KV Cache分离和跨节点池化，HCCS是天然优势

### 演讲话术（60-90秒）

> "Dynamo做路由，LMCache做共享，但更根本的问题是——KV Cache绑定在单GPU上，扩展性受单机显存硬约束。Mooncake从架构层彻底解决。
>
> 这是月之暗面Kimi的生产系统架构，不是学术论文，是真实线上流量验证过的方案。核心思路：以KV Cache为中心做disaggregated architecture——把KV Cache从GPU上解耦出来，通过RDMA跨节点传输，存入分布式KV Store。
>
> 三个关键设计：第一，KVCache-centric的分离架构，KV Cache不再绑定在生成它的GPU上，可以自由迁移和池化。第二，RDMA加分布式KV Store，高带宽低延迟的跨节点KV传输。第三，Prefill-Decode分离——Prefill是计算密集的，Decode是访存密集的，放在一起互相干扰，分开后各自优化。
>
> 关键逻辑：这不是一篇学术论文提出的概念，而是月之暗面在Kimi的生产环境中实际运行的架构，已经被真实流量验证。这对我们的启示——NPU推理架构需要支持KV Cache分离和跨节点池化，而HCCS互联恰好是这方面的天然优势。
>
> Routing环节小结：四个技术层层递进——ProRL Agent做Rollout解耦释放GPU，Dynamo做KV感知路由，LMCache做跨引擎共享，Mooncake做KV Cache分离池化。框架层是技术密度最高的一层。接下来沿R3数据流进入Record环节——请注意，那里的叙事会很不一样。"

---

## P14 | Record环节：环境保真与工程可达（C6+C7）— Forge + 行业空白

### PPT内容

**标题**：Record环节：基础设施层的行业空白 — 唯一方案Forge + 我们的机会

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：Record环节：基础设施层的行业空白                                  │
│  副标题：唯一方案Forge + 我们的机会                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  问题描述区（左侧 60%）             空白示意（右侧 40%）     │     │
│  │                                                            │     │
│  │  Record环节承载两个挑战：        ┌────────┐ ←→ ┌──────┐  │     │
│  │                                   │探索效率 │     │安全性│  │     │
│  │  C6 环境保真：sandbox偏差          └────────┘     └──────┘  │     │
│  │  导致训练信号失真。Agent在                                   │     │
│  │  探索中可能陷入死循环、冗余          算法层：3个方案 ●●●      │     │
│  │  操作、永不终止——消耗算力           框架层：4个方案 ●●●●     │     │
│  │  却无有效信号。                     基础设施层：              │     │
│  │                                    1个方案 ● + ⚠️ 空白     │     │
│  │  C7 工程可达：算法2-4周迭代，                                │     │
│  │  但算子适配3-6个月。开放任务                                  │     │
│  │  的奖励定义模糊，并行探索越多                                 │     │
│  │  越容易发现 reward hack 捷径。                               │     │
│  │                                                            │     │
│  │  回看P4的3-4-1分布：基础设施层最薄弱                         │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌──────────────────────────────────────┐  ┌──────────────────────┐ │
│  │  唯一方案：MiniMax Forge分层设计       │  │  ⚠️ 行业空白         │ │
│  │                                      │  │                      │ │
│  │  解决C7工程可达：                      │  │  sandbox保真度验证   │ │
│  │  · Agent脚手架与训练引擎完全解耦       │  │  可控探索平台         │ │
│  │  · 支持任意Agent脚手架+工具            │  │  大规模环境编排       │ │
│  │  · 树状样本合并策略→40x加速            │  │  reward hack检测     │ │
│  │  · M2.5: 20万+真实环境训练             │  │                      │ │
│  │    日吞吐百万级, SWE-Bench 80.2%      │  │  尚无生产级方案       │ │
│  │                                      │  │  → 见P15：这正是     │ │
│  │  但Forge只解决了C7（工程可达），       │  │    我们的切入机会     │ │
│  │  C6（环境保真）仍是空白               │  │                      │ │
│  └──────────────────────────────────────┘  └──────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  关键逻辑（高亮条）：                                         │    │
│  │  基础设施层=行业最薄弱环节。Forge解决C7但C6仍空白。             │    │
│  │  算法层和框架层卷得厉害，基础设施层几乎没人做——                  │    │
│  │  这恰恰是硬件平台方的天然主场。                                │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  启示：NPU作为硬件平台方，在基础设施层（sandbox保真、           │    │
│  │  可控探索、精度一致性）有天然优势——这不是追赶，是卡位。         │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "R3数据流的第三站：Record环节——记录轨迹，这是基础设施层。但请注意，这一页的叙事跟前面不同——不是展开技术，而是**指出空白**。
>
> 回看P4的3-4-1分布：算法层3个方案，框架层4个方案，基础设施层只有1个加一个醒目的空白。
>
> 这个环节承载C6环境保真和C7工程可达。先看C6——sandbox偏差导致训练信号失真，Agent在探索中可能陷入死循环、冗余操作、永不终止，消耗算力却无有效信号。C7——算法2到4周迭代，但算子适配要3到6个月，开放任务的奖励定义模糊，并行探索越多越容易发现reward hack捷径。
>
> 唯一的方案是MiniMax的Forge分层设计——Agent脚手架与训练引擎完全解耦，树状样本合并策略实现40倍加速，M2.5在20万以上真实环境中训练，SWE-Bench达到80.2%。但Forge只解决了C7工程可达，C6环境保真——sandbox保真度验证、可控探索平台、大规模环境编排——仍然是**行业空白**。
>
> 这对我们意味着什么？算法层和框架层已经被ByteDance、NVIDIA、MiniMax等巨头占满了，我们从零追赶。但基础设施层——sandbox保真、可控探索、精度一致性——几乎没人做。这不是追赶，是卡位。作为硬件平台方，我们在这个层面有天然优势。
>
> R3数据流三个环节展开完毕。最后一页，收束我们的定位和行动。"

---

## P15 | 收束层：全栈协同 — 对我们团队的启示

### PPT内容

**标题**：全栈协同：三层架构调整 x R3数据流 x 6挑战全覆盖

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题：全栈协同：三层架构调整 × R3数据流 × 6挑战全覆盖                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  核心判断（大字，居中，高亮背景）：                              │    │
│  │  6挑战+横切约束沿R3数据流穿透三层架构                         │    │
│  │  三层架构调整 × 全栈协同 = 我们"帮客户开箱即用"的价值锚点       │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐    │
│  │ 🔵行动方向 ①      │ │ 🟢行动方向 ②      │ │ 🟠行动方向 ③      │    │
│  │ Reply：           │ │ Routing：         │ │ Record：          │    │
│  │ 训推协同基础设施   │ │ 有状态推理引擎     │ │ 可控探索平台       │    │
│  │                  │ │                  │ │                  │    │
│  │ 对应挑战：        │ │ 对应挑战：        │ │ 对应挑战：        │    │
│  │ C1 策略时效性     │ │ C2 采集吞吐       │ │ C6 环境保真       │    │
│  │ C4 奖励与归因     │ │ C3 推理效率       │ │ C7 工程可达       │    │
│  │                  │ │                  │ │                  │    │
│  │ 关键能力：        │ │ 关键能力：        │ │ 关键能力：        │    │
│  │ · 异步训练调度    │ │ · Rollout解耦    │ │ · sandbox保真    │    │
│  │ · 跨节点通信优化  │ │ · KV cache 共享  │ │ · 可控探索平台   │    │
│  │ · 策略版本管理    │ │   /迁移能力      │ │ · 框架级分层解耦 │    │
│  │ · step-level归因 │ │ · 互联架构优化    │ │ · 大规模环境编排 │    │
│  │ · 闭环训练加速    │ │   (HCCS)        │ │ · 精度一致性     │    │
│  │ · off-policy IS  │ │ · 多级内存管理    │ │   (天然优势)     │    │
│  │ 技术参考：        │ │                  │ │ 技术参考：        │    │
│  │ Decoupled PPO /  │ │ 技术参考：        │ │ Forge分层设计     │    │
│  │ DAPO+CISPO /     │ │ ProRL Agent /    │ │ ⚠️ 行业空白=     │    │
│  │ Cursor+Meta SSR  │ │ Dynamo / LMCache │ │   我们的切入机会  │    │
│  │                  │ │ / Mooncake       │ │                  │    │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  ⚪横切约束 C5（数值可信）── 穿透三个行动方向的共同底线         │    │
│  │  NPU精度+训推一致性 = 全栈协同的基础                            │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  收束（大字，高亮条）：                                        │    │
│  │  Agent 时代的竞争，不在谁的模型更大，而在谁的全栈更顺。         │    │
│  │  我们的机会：让 NPU 成为客户做 Agentic RL 后训练的最优选择。   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 演讲话术（60-90秒）

> "最后收束。六个挑战加横切约束，沿R3数据流穿透三层架构。但今天汇报最核心的判断不只是'需要全栈协同'——而是**行业空白在哪里，我们该卡在哪里**。
>
> 请看三个行动方向。
>
> 行动方向一：Reply环节——训推协同基础设施，对应C1策略时效性和C4奖励与归因。关键能力是异步训练调度、跨节点通信优化、策略版本管理，以及off-policy IS修正。参考 Decoupled PPO、DAPO+CISPO、Cursor实时RL+Meta SSR。
>
> 行动方向二：Routing环节——有状态推理引擎，对应C2采集吞吐和C3推理效率。关键能力是 Rollout解耦、KV cache 共享和迁移、HCCS 互联架构优化、多级内存管理。参考 ProRL Agent、Dynamo、LMCache、Mooncake——这一层技术最密集，也是我们推理引擎团队最有基础快速跟进的。
>
> 行动方向三：Record环节——**可控探索平台，这是我们的差异化主战场**。对应C6环境保真和C7工程可达。目前业界只有Forge一个方案，sandbox保真和可控探索仍是行业空白。作为硬件平台方，我们在精度一致性、sandbox保真度、大规模环境编排方面有天然优势——这不是追赶，是卡位。
>
> 请注意底部这行——横切约束C5数值可信，穿透三个行动方向的共同底线。NPU上的算子精度和训推一致性是全栈协同的基础。如果底层数值不可信，上层所有优化都建立在沙滩上。这也是我们作为硬件平台方独有的优势。
>
> 特别强调——这次技术参考全部来自工业巨头的生产级实践：ByteDance的DAPO、MiniMax的CISPO和Forge、NVIDIA的ProRL Agent、Cursor和Meta的闭环训练。不是学术论文展望，是已经在线上跑、有真实数据验证的方案。
>
> 910C 和 H100 的裸性能差距是 60%，但如果我们在精度和一致性上做到业界最好——确定性计算、批不变性、训推一致性——客户会因为训练结果可复现而选择我们，而不仅仅因为 FLOPS 便宜。
>
> 底线判断：**Agent 时代的竞争，不在谁的模型更大，而在谁的全栈更顺。三层架构调整沿R3数据流覆盖全部六个挑战加横切，这就是我们的全景作战图。我们的机会是让 NPU 成为客户做 Agentic RL 后训练的最优选择——不是 GPU 的替代品，而是全栈优化的差异化平台。**
>
> 今天汇报的核心诉求：Agent RL 后训练是确定性的算力需求方向，我们的技术布局已经对准了R3数据流上的六大挑战，需要资源支持加速落地。谢谢各位领导。"

---

## 附：页码总览与逻辑流

```
P1  Agent时代已来                              ← 定调：产品已落地+开源引爆+巨头真金白银
P2  演进+不可替代性（合并）                      ← 动因：三阶段演进+排除法+桥接层+飞轮+巨头验证
P3  七维→五挑战+横切（直接展示）                 ← 推导：七维对比→5挑战直接正推+横切
P4  五挑战→七升级方向（因果传导）+三层架构图     ← 导航：因果传导表（因为→所以→否则）+三层架构
P5  基础设施层升级（挑战❶+❷驱动）               ← KV池化+推理加速+sandbox并行（3个技术）
P6  Decoupled PPO                              ← Off-Policy基础（actor-learner分离+V-trace/IS修正）
P7  DAPO+CISPO                                 ← 工业级异步RL（ByteDance+MiniMax）
P8  Cursor实时RL+Meta SSR                      ← 闭环训练+数据层（极快闭环×自博弈）
P9  框架层升级（挑战❷+❸驱动）                   ← 训推解耦+异步调度（4个技术）
P10 ProRL Agent                                ← Rollout解耦层（NVIDIA，框架级架构解耦）
P11 Dynamo                                     ← 推理调度层（KV感知路由）
P12 LMCache                                    ← 存储缓存层（跨引擎P2P共享）
P13 算法层升级（挑战❹+❺驱动）                   ← off-policy容忍+步级归因
P14 Record环节（环境保真+工程可达）              ← Forge+行业空白（基础设施层1+行业空白）
P15 全栈协同·团队启示                            ← 收束：三层全面升级+横切=我们的卡位机会
```

**页间逻辑链**：现象(P1) → 论证why(P2) → 推导5挑战+横切(P3) → 因果传导+三层架构(P4) → 按三层逐层展开(P5-P14) → 收束行动(P15)

**五挑战→七升级方向→三层映射（v7更新）**：
- 基础设施层（挑战❶+❷驱动/3个方向）：a)KV Cache分布式池化 b)推理加速 c)sandbox并行化 → P5-P8
  - LMCache/Mooncake、Eagle3/4bit量化、Forge
- 框架层（挑战❷+❸驱动/2个方向）：d)训推解耦 e)异步流水线+动态调度 → P9-P12
  - ProRL Agent、Dynamo
- 算法层（挑战❹+❺驱动/2个方向）：f)off-policy容忍+多样性探索 g)步级奖励+精细归因 → P13
  - Decoupled PPO/DAPO+CISPO、Cursor实时RL+Meta SSR
- 横切（数值精度保证）：训推一致性/确定性计算/精度基线/算子自动生成

**五挑战体系（v7更新）**：
1. ❶ 长序列推理开销（维度①+⑦）
2. ❷ 环境交互延迟（维度②+③）
3. ❸ 在线数据吞吐瓶颈（维度④）
4. ❹ 探索效率（维度⑥）
5. ❺ 稀疏延迟奖励的归因（维度⑤）
+ 横切：数值可信

**数据校准说明**：
- GitHub 星数：OpenClaw 24.7万（原始PPT数据）
- Anthropic RL 环境投入：表述为"讨论年投$1B+"（The Information 报道，非官方确认）
- 后训练算力 40%+：指广义后训练（含 RLHF+蒸馏+合成数据等）
- SFT 天花板：已修正为"训练分布≠部署分布 (distribution shift)"（P2P3-final.md 三方共识）
- 对比维度：七维（含"上下文"维度）
- 挑战体系：从6挑战+1横切(C1-C7)简化为5挑战+横切，按维度正推
- 升级方向：7个方向按因果链（因为→所以→否则）组织，按三层架构分组
- 技术分布：基础设施层3个方向、框架层2个方向、算法层2个方向+横切
